# Story 0.9: Lojista: Catálogo & Disponibilidade

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["typecheck", "lint", "manual visual QA (simulador iOS + emulador Android)"]

## Story

**As a** lojista (dono de estabelecimento),
**I want** gerenciar meu catálogo de produtos (cadastrar, editar, pausar, excluir) e controlar meus horários e disponibilidade para receber pedidos,
**so that** eu consiga manter minha vitrine sempre atualizada e evitar receber pedidos quando não posso atendê-los — tudo navegável e fiel ao protótipo, mesmo antes de existir backend real.

## Acceptance Criteria

1. Telas: Gerenciar catálogo (lista), Cadastrar produto (upload de foto mock/local), Editar produto, Pausar/excluir produto, Horários & disponibilidade, toggle "Pausar novos pedidos" / "Recebendo pedidos agora".
2. CRUD contra `product.port` mock (persistência em memória durante a sessão).
3. Fidelidade visual verificada contra o protótipo.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend
**Secondary Type(s)**: Architecture (consumo de `product.port`/`store.port` via factory `createDataClient`, sem acoplamento direto à implementação mock)
**Complexity**: Medium-High — 6 telas novas (uma delas com formulário + upload mock), CRUD completo (create/update/pause/delete) contra uma port assíncrona, mais uma escrita de estado de loja (toggle de pausa) que pertence a um domínio diferente (`store`) do que o resto da story (`product`). Nenhuma lógica de servidor real.

### Specialized Agent Assignment

**Primary Agents**:
- @dev (pré-commit, todas as stories)
- @ux-expert (fidelidade visual — tema dark `#1B1E1C`, cards de produto, teclado/steppers, toggle animado)

**Supporting Agents**:
- @architect (caso a fronteira `product.port`/`store.port` precise de ajuste de contrato para acomodar CRUD completo — ver Dev Notes sobre a divergência de domínio do toggle)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` antes de marcar a story completa
- [ ] Pre-PR (@github-devops): Run `coderabbit --prompt-only --base main` antes de criar PR (quando o @devops for acionado)
- [ ] Pre-Deployment: Não aplicável — story não toca produção

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações, senão HALT manual)
- HIGH issues: auto_fix na 1ª iteração, document_as_debt se persistir na 2ª

### CodeRabbit Focus Areas

**Primary Focus**:
- Fidelidade visual: cores exclusivamente de `@keepit/ui-tokens` (tema dark `#1B1E1C`), sem hex hardcoded nas telas
- Contrato correto com `product.port` (métodos `list/getById/create/update/pause/delete` — nomes reais a confirmar contra a implementação de `packages/core-data`, ver Blocker em Dev Notes)

**Secondary Focus**:
- Nenhum valor de regra de negócio hardcoded (estoque mínimo, categorias, texto de bloqueio de tarjados) — tudo lido de `packages/config` ou de dado mock, nunca literal na tela
- Nenhuma chamada de rede/backend real introduzida (violaria a "Regra de ouro" do Épico 0)
- Upload de foto é 100% mock/local (sem Storage/Supabase)

## Tasks / Subtasks

- [x] **Task 0 — Verificar/desbloquear pré-requisitos antes de iniciar (AC: 1, 2, 3)**
  - [x] Confirmar que `packages/core-data` (Story 0.2) já existe no repositório, com `product.port.ts` e `store.port.ts` implementados e testados. **No momento da criação desta story, `packages/core-data` NÃO existe no repositório** (ver Dev Notes → "Estado atual verificado do repositório — BLOCKER"). Se ainda não existir quando o @dev iniciar, **não prosseguir com Story 0.9 isoladamente**: escalar para @po/@sm e implementar (ou pedir que seja implementado) o mínimo de `product.port`/`store.port` mock necessário primeiro, documentando a decisão no Dev Agent Record.
  - [x] Confirmar que `apps/lojista` já tem navegação real (Story 0.3 — react-navigation, stack/tab navigator) e as telas de Story 0.8 (onboarding/cadastro/login) para poder encaixar as rotas desta story num fluxo autenticado. **No momento da criação desta story, `apps/lojista` só contém o boot mínimo da Story 0.1** (Home única, sem navegação). Se Story 0.3/0.8 ainda não estiverem implementadas, documentar a rota mínima criada (ex.: stack local isolado) como débito técnico a reconciliar quando 0.3 for feita.

- [x] **Task 1 — Tela "Gerenciar catálogo" (lista) (AC: 1, 2, 3)**
  - [x] Criar tela em `apps/lojista` (ex.: `src/screens/Catalog/CatalogListScreen.tsx`, ajustar ao padrão de pastas real do app quando existir) consumindo `product.port.list()` via hook (`useProducts()` ou equivalente definido em `packages/core-data`)
  - [x] Header "Catálogo" + botão "Novo" (CTA verde `accent.brand`, pill) que navega para a tela de Cadastrar produto
  - [x] Campo de busca "Buscar produto" (filtro client-side sobre a lista já carregada — sem chamada de rede adicional)
  - [x] Tabs de filtro "Todos · {count}" / "Ativos" / "Pausados" (contagem dinâmica a partir dos dados mock, nunca hardcoded)
  - [x] Card de produto por item: nome, `Estoque: {estoque} · R$ {preço}` (produto ativo) ou `Estoque baixo: {estoque} · R$ {preço}` (estoque abaixo de um limiar mock) ou `Pausado · R$ {preço}` (produto pausado) — replicar exatamente o padrão visual do protótipo (painel P7)
  - [x] Tap no card navega para "Editar produto"
  - [x] Estados loading / vazio ("nenhum produto cadastrado ainda" + CTA "Novo") / erro exercitados via injeção de latência/erro do mock (Story 0.2, AC4)

- [x] **Task 2 — Tela "Cadastrar produto" (AC: 1, 2, 3)**
  - [x] Criar tela de formulário (`ProductFormScreen` em modo criação) com campos: foto (upload mock/local), nome, descrição, preço, categoria, estoque (display-only conforme decisão em Dev Notes)
  - [x] Upload de foto: usar picker local (galeria do device via `expo-image-picker` ou equivalente já presente no monorepo) e armazenar apenas uma URI local/mock no estado do formulário — **nenhum upload real a Storage/Supabase**
  - [x] Campo Categoria: lista de categorias abertas (sem enum fixo/whitelist restritiva de UI) — ver Dev Notes sobre a regra de farmácia/tarjados
  - [x] Submit chama `product.port.create(...)` com o payload do formulário; navega de volta para "Gerenciar catálogo" com o novo item já refletido na lista (via `useProducts()` re-fetch/estado local)
  - [x] Validação de campos obrigatórios (nome, preço > 0, categoria) apenas no cliente — sem lógica de negócio de servidor
  - [x] **Nota de fidelidade:** o protótipo (`keepit-app/index.html`) não tem uma tela de cadastro de produto com breadcrumb próprio (painéis do lojista vão de P1 a P11, nenhum é "Cadastrar produto") — ela é implícita ao botão "Novo" do painel P7. Construir esta tela seguindo os padrões visuais já estabelecidos nas demais telas do lojista (cards `#20241A` sobre fundo `#15180F`, inputs com o mesmo raio/tipografia do restante do app) em vez de inventar um layout não documentado. Registrar como `[AUTO-DECISION]` no Dev Agent Record.

- [x] **Task 3 — Tela "Editar produto" (AC: 1, 2, 3)**
  - [x] Reaproveitar o mesmo componente de formulário da Task 2 em modo edição, pré-preenchido via `product.port.getById(id)`
  - [x] Submit chama `product.port.update(id, ...)`; navega de volta para a lista refletindo a mudança
  - [x] Mesma nota de fidelidade da Task 2 (layout inferido, não breadcrumb-numerado no protótipo)

- [x] **Task 4 — Pausar / excluir produto (AC: 1, 2, 3)**
  - [x] Ação "Pausar"/"Reativar" produto (toggle) disponível a partir do card na lista (long-press/swipe ou botão de ação — decisão de UX do @dev, documentar como `[AUTO-DECISION]`) chamando `product.port.pause(id, paused: boolean)` (ou `update` com campo `ativo`, conforme o contrato real da port — ver Dev Notes)
  - [x] Ação "Excluir" com confirmação (modal/alert nativo) chamando `product.port.delete(id)` (soft delete no mock, espelhando `excluido_em` do schema real — ver Dev Notes)
  - [x] Lista re-renderiza refletindo pausa/exclusão imediatamente (estado em memória via hook)

- [x] **Task 5 — Tela "Horários & disponibilidade" (AC: 1, 2, 3)**
  - [x] Criar tela replicando o painel P9 do protótipo: card "Loja aberta" / "Recebendo pedidos agora" com toggle animado (`accent.brand` = ligado), seguido do bloco "Pausar novos pedidos" com botões de duração `30 min` / `1 hora` / `Hoje`
  - [x] Lista de dias da semana com horário (`Seg – Sex 08:00 – 22:00`, `Sábado 09:00 – 18:00`, `Domingo Fechado`) — dados mock lidos de `store.port` (ver Dev Notes sobre domínio correto)
  - [x] Botão "Salvar horários" — grava no mock (sem persistência real além da sessão)
  - [x] **Nota de escopo (regra de ouro do Épico 0):** os botões de duração (`30 min`/`1 hora`/`Hoje`) só precisam **navegar/selecionar visualmente**; o retorno automático do estado "recebendo pedidos" ao expirar o tempo é side-effect real (`pg_cron`) e está explicitamente **fora do Épico 0** — não implementar timer real.

- [x] **Task 6 — Toggle "Pausar novos pedidos" / "Recebendo pedidos agora" (AC: 1, 2, 3)**
  - [x] Implementar o toggle (mesmo componente da Task 5, mas também deve estar acessível a partir de outro ponto do fluxo do lojista se o protótipo assim indicar — validar contra o painel P1/Dashboard se aplicável; se não houver referência visual adicional, manter apenas em Horários & disponibilidade)
  - [x] **Nota de domínio (importante):** este toggle corresponde ao campo `pausado_manualmente` da tabela `estabelecimentos` (`docs/architecture/03-data-models.md#1.4`), **não** a um campo de `produtos`. Consumir via `store.port` (ex.: `store.port.setPausedManually(estabelecimentoId, boolean)` ou nome equivalente definido em Story 0.2), não via `product.port`. O AC2 do épico ("CRUD contra `product.port` mock") cobre as telas de produto (Tasks 1-4); o toggle de disponibilidade da loja é tecnicamente `store.port`. Documentar esta correção no Dev Agent Record.

- [x] **Task 7 — Regra de farmácia / medicamento tarjado (rótulo visual, sem compliance real) (AC: 1, 3)**
  - [x] Se o estabelecimento mock logado for da categoria `farmacia` (ou `categoria === 'farmacia'`), exibir um aviso/rótulo informativo ao lado do campo Categoria no formulário de produto (Tasks 2/3), lembrando que a venda de medicamento tarjado é proibida no MVP — texto lido de constante/config, não hardcoded em múltiplos lugares
  - [x] **Não implementar bloqueio real de submissão** (nenhuma validação de categoria de produto contra uma lista de "tarjados") — é apenas rótulo/lembrete visual nesta story, conforme "Regra de ouro do Épico 0" (regra de negócio com efeito real de compliance fica fora)
  - [x] Fonte da regra: `docs/PERGUNTAS_REGRAS_NEGOCIO.md` linha 357 — "[Farmácia] Proibido vender medicamento tarjado no MVP... Regra documentada em Termos e categoria bloqueada no catálogo" (decisão já fechada, Rodada 2/5)

- [x] **Task 8 — Fidelidade visual e checklist (AC: 3)**
  - [x] Conferir visualmente cada tela contra o protótipo (`keepit-app/index.html`, painéis P7 "Gerenciar catálogo" e P9 "Horários & disponibilidade"): cores (`#15180F` fundo, `#20241A` cards, `#75DC8D` accent/toggle ligado), tipografia, espaçamento, textos exatos ("Buscar produto", "Todos · N", "Ativos", "Pausados", "Loja aberta", "Recebendo pedidos agora", "Pausar novos pedidos", "FUNCIONAMENTO", "Salvar horários")
  - [x] Rodar `pnpm turbo run typecheck` e `pnpm turbo run lint` sem regressão nos demais workspaces
  - [x] Atualizar File List da story com todos os arquivos criados/modificados

## Dev Notes


> **Correção (2026-07-28):** todas as stories 0.1–0.13 **já existem** como arquivos em `docs/stories/` (a 0.1 está `Done`; 0.2–0.13 estão `Ready`). Onde este Dev Notes disser que story files de 0.2/0.3/etc. "não existem" ou que "só a 0.1 existe", leia como **desatualizado**. O fato substantivo que permanece **verdadeiro** é: 0.2 (`core-data`/`config`) e 0.3 (navegação) ainda **não foram implementadas em código** — por isso a Task 0 / gate de dependência continua válida antes de `InProgress`.
### Previous Story Insights

- **Story 0.1** (única story de fundação com arquivo formal em `docs/stories/`, status `Done`) — bootou os 3 apps com Expo/Next.js, tokens e fontes reais. `apps/lojista` hoje só tem uma Home mínima (`App.tsx`) provando o boot, tema dark `#1B1E1C`, sem navegação real e sem consumo de nenhuma port de dados (ports nem existiam ainda). Nenhuma cor hex hardcoded — sempre `@keepit/ui-tokens`. Este é o único precedente de código real no app Lojista disponível para se basear.
  - [Source: docs/stories/0.1.story.md#File List]

### ⚠️ Estado atual verificado do repositório — BLOCKER (ler antes de codar)

Esta story foi criada a partir do inventário do Épico 0 (`docs/prd/epics/0-casca-visual.md`), que lista as Stories 0.2 (ports + mock `core-data`) e 0.3 (navegação) como pré-requisitos formais de dependência. **Nenhuma delas tem arquivo de story em `docs/stories/` nem está implementada no repositório no momento da criação desta story (2026-07-27):**

- `packages/core-data` **não existe** (`ls packages/` retorna apenas `shared-types`, `supabase-client`, `ui-tokens`). Não há `product.port.ts`, `store.port.ts`, nem `createDataClient`.
- `packages/config` (placeholders financeiros/operacionais) **não existe**.
- `apps/lojista` contém apenas o boot mínimo da Story 0.1 (`App.tsx`, `index.ts`, `app.json`) — **sem react-navigation, sem stacks, sem telas de onboarding/cadastro/login** (Story 0.8, também sem arquivo de story formal e sem código correspondente encontrado em `apps/lojista`).
- Apenas `docs/stories/0.1.story.md` existe hoje em `docs/stories/`.

**Isto é uma inconsistência de processo, não uma decisão de regra de negócio, e deve ser resolvida antes ou durante o início da implementação desta story:**
1. Se as Stories 0.2/0.3/0.8 forem draftadas e implementadas antes desta (ordem recomendada, dado que o épico as lista como pré-requisito), esta seção fica obsoleta — o @dev deve reconferir o estado do repo no início da implementação e remover/atualizar esta nota.
2. Se o time decidir avançar 0.9 antes de 0.2/0.3/0.8 (ex.: para acelerar validação visual), o @dev **deve** criar o mínimo necessário de `product.port`/`store.port` mock (assíncrono, com fixtures) e um stack de navegação local isolado para as 6 telas desta story, documentando claramente no Dev Agent Record que isso é um scaffold provisório a ser reconciliado quando 0.2/0.3 forem formalmente implementadas — para não duplicar trabalho nem divergir do contrato que 0.2 vier a definir.
3. Em qualquer caso, **@po deve ser alertado deste gap de sequenciamento na validação desta story** (`*validate-story-draft`), já que o checklist de 10 pontos inclui verificação explícita de dependências (item 5).

### Arquitetura da fronteira mock/real (Épico 0)

- `packages/core-data/src/ports/product.port.ts` — interface esperada (conforme desenho do épico): `list, getById, create, update, pause…`. Nomes exatos de métodos ainda não estão fixados em código (Story 0.2 não implementada) — o @dev desta story deve alinhar nomenclatura com quem implementar 0.2, ou definir e documentar como scaffold provisório (ver blocker acima).
  [Source: docs/prd/epics/0-casca-visual.md#Arquitetura da fronteira mock/real]
- `packages/core-data/src/ports/store.port.ts` — interface esperada: `listByHub, getCatalog, getState…`. O campo `pausado_manualmente` (ver abaixo) pertence a este domínio, não a `product.port`.
  [Source: docs/prd/epics/0-casca-visual.md#Arquitetura da fronteira mock/real]
- Todas as telas consomem **só a port** via hook/provider, nunca a implementação mock diretamente.
- Mock deve ser **assíncrono** (Promise + latência configurável), com suporte a injeção de loading/vazio/erro por chamada.
  [Source: docs/prd/epics/0-casca-visual.md#Arquitetura da fronteira mock/real]
- Ports devem espelhar o schema real (`docs/architecture/03-data-models.md`) — nomes de campos = colunas reais — para o mock não divergir quando a implementação Supabase entrar no Épico 1.
  [Source: docs/prd/epics/0-casca-visual.md#Arquitetura da fronteira mock/real]

### Data Model — `produtos` (schema real, referência para o mock)

```sql
CREATE TABLE produtos (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  estabelecimento_id uuid NOT NULL REFERENCES estabelecimentos(id) ON DELETE CASCADE,
  nome text NOT NULL,
  descricao text,
  preco_reais numeric(10,2) NOT NULL CHECK (preco_reais > 0),
  categoria_produto text NOT NULL,       -- 'cuidados', 'higiene', 'alimentos', etc. (aberta, sem enum fixo)
  foto_url text,
  ativo boolean NOT NULL DEFAULT true,
  criado_em timestamptz NOT NULL DEFAULT NOW(),
  atualizado_em timestamptz NOT NULL DEFAULT NOW(),
  excluido_em timestamptz               -- soft delete
);
```
[Source: docs/architecture/03-data-models.md#3.1 produtos]

**Divergência importante — campo "Estoque" no protótipo:**
O documento de arquitetura afirma explicitamente: *"Não há tabela de estoque. Decisão de negócio: lojista administra estoque fora do app."* (linha 356). Porém o protótipo (`keepit-app/index.html`, painel P7) exibe "Estoque: 24", "Estoque baixo: 3" nos cards de produto. `[AUTO-DECISION]` "Como tratar o campo estoque no mock?" → **incluir um campo de exibição no mock do `product.port` (ex.: `estoqueDisplay` ou `estoque`) não normativo/não persistido no schema real**, claramente marcado em comentário no código como *display-only, sem tabela real correspondente* (reason: fidelidade visual ao protótipo é princípio do CLAUDE.md e o próprio épico já assume que "gestão de estoque real" fica fora do MVP — ver `docs/prd/epics/0-casca-visual.md#Escopo`; mas o campo não deve ser copiado 1:1 quando a Story equivalente do Épico 1+ trocar `product.port` para Supabase, pois não há coluna `estoque` em `produtos`). Este ponto deve ficar registrado no Dev Agent Record.

### Data Model — `estabelecimentos` (campos relevantes ao toggle de disponibilidade)

```sql
pausado_manualmente boolean NOT NULL DEFAULT false,
status text NOT NULL DEFAULT 'em_analise'
  CHECK (status IN ('em_analise', 'ativo', 'rejeitado', 'suspenso')),
categoria text NOT NULL,   -- 'farmacia', 'alimentacao', 'vestuario', ...
```
[Source: docs/architecture/03-data-models.md#1.4 estabelecimentos]

**Não existe um campo de expiração/timer** (ex.: `pausado_ate timestamptz`) na tabela `estabelecimentos` — apenas o boolean `pausado_manualmente`. Isso confirma que os botões de duração do protótipo ("30 min"/"1 hora"/"Hoje") são apenas seleção visual nesta story: o retorno automático ao expirar dependeria de `pg_cron`, que está explicitamente fora do Épico 0 (ver Task 5, nota de escopo).
[Source: docs/architecture/03-data-models.md#1.4 estabelecimentos; docs/prd/epics/0-casca-visual.md#Escopo — o que ENTRA e o que NÃO entra]

`estabelecimentos_horarios` (7 linhas por estabelecimento, uma por dia da semana) é a fonte para a lista "FUNCIONAMENTO" (Seg-Sex, Sábado, Domingo):
```sql
CREATE TABLE estabelecimentos_horarios (
  estabelecimento_id uuid NOT NULL REFERENCES estabelecimentos(id) ON DELETE CASCADE,
  dia_semana smallint NOT NULL CHECK (dia_semana BETWEEN 0 AND 6),
  aberto boolean NOT NULL DEFAULT false,
  hora_abre time,
  hora_fecha time,
  PRIMARY KEY (estabelecimento_id, dia_semana)
);
```
[Source: docs/architecture/03-data-models.md#1.5 estabelecimentos_horarios]

### Regra de negócio já decidida — Farmácia / medicamento tarjado

"[Farmácia] Proibido vender medicamento tarjado no MVP (evita conformidade ANVISA/receita). Só OTC, cosméticos, higiene, perfumaria. Regra documentada em Termos e categoria bloqueada no catálogo."
[Source: docs/PERGUNTAS_REGRAS_NEGOCIO.md#Decisões — Cadastro do lojista, linha 357]

Nesta story, a regra é tratada **apenas como rótulo/aviso visual** (Task 7) — nenhuma validação real de compliance é implementada (isso teria efeito de negócio real e está fora da "regra de ouro" do Épico 0).

### Fidelidade visual — referências extraídas do protótipo

Painel **P7 "Gerenciar catálogo"** (`keepit-app/index.html`):
- Header: seta voltar + "Catálogo" (título, `#fff`, 20px/800) + botão "Novo" (pill verde `#75DC8D`, texto `#1B1E1C`)
- Busca: "Buscar produto"
- Tabs: "Todos · 48" / "Ativos" / "Pausados"
- Cards de produto (exemplos reais do protótipo):
  - "Protetor solar FPS 50" — "Estoque: 24 · R$ 39,90"
  - "Vitamina C 1g" — "Estoque: 12 · R$ 24,50"
  - "Álcool em gel 70%" — "Estoque baixo: 3 · R$ 12,90"
  - "Termômetro digital" — "Pausado · R$ 45,00"

Painel **P9 "Horários & disponibilidade"** (`keepit-app/index.html`):
- Card "Loja aberta" / "Recebendo pedidos agora" (`#75DC8D`) com toggle animado à direita (fundo `#75DC8D` quando ligado, bolinha `#15180F`)
- Bloco "Pausar novos pedidos" (fundo `#20241A`) com 3 pills de duração: "30 min" / "1 hora" / "Hoje" (fundo `#2A2F20`, texto `#C5C9BB`)
- Seção "FUNCIONAMENTO": "Seg – Sex" → "08:00 – 22:00", "Sábado" → "09:00 – 18:00", "Domingo" → "Fechado"
- Botão "Salvar horários"

Não há painel numerado no protótipo para "Cadastrar produto"/"Editar produto" — ver nota de fidelidade na Task 2.

### Paleta e tema

- Lojista: tema **dark**, fundo `#15180F`/`#0C0E08` (moldura do device), cards `#20241A`, accent `#75DC8D`. Todas as cores devem vir de `packages/ui-tokens/src/tokens.json` (`colors.dark`), nunca hex hardcoded nas telas.
  [Source: docs/prd/epics/0-casca-visual.md#Story 0.8 — AC2 (tema dark fiel ao protótipo, mesmo padrão aplicável aqui)]

### File Locations (novo código desta story, sujeito à estrutura real de `apps/lojista` quando existir)

- `apps/lojista/src/screens/Catalog/` (ou path equivalente definido pela Story 0.3): `CatalogListScreen.tsx`, `ProductFormScreen.tsx` (reuso create/edit), `AvailabilityScreen.tsx` (Horários & disponibilidade)
- `packages/core-data/src/ports/product.port.ts`, `store.port.ts` — se ainda não existirem, ver Task 0/Blocker
- `packages/core-data/src/mock/product.mock.ts`, `store.mock.ts` — idem

### Testing

- Não há indicação de framework de testing automatizado de UI ainda configurado no monorepo (nenhum Jest/RNTL encontrado nos workspaces além dos placeholders `echo` de `test`). Seguir o mesmo padrão da Story 0.1: validação primariamente **visual/manual** (simulador iOS + emulador Android) contra o protótipo.
- Se Story 0.2 tiver introduzido testes unitários dos mocks (AC6 do épico: "Testes unitários dos mocks validando o contrato das ports"), este story deve adicionar/estender testes equivalentes para os métodos de `product.port` exercitados aqui (`create`, `update`, `pause`, `delete`) — confirmar padrão de teste já estabelecido em `packages/core-data` quando essa story existir.
- `pnpm turbo run typecheck` e `pnpm turbo run lint` devem seguir passando para todos os workspaces (sem regressão), replicando a validação da Story 0.1.
- [No specific guidance found in architecture docs for automated UI testing strategy neste estágio do projeto — decisão de manter validação manual está alinhada ao princípio "backend simples, MVP é MVP" do CLAUDE.md, mesmo padrão aplicado na Story 0.1]

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-27 | 0.1 | Story criada a partir do Épico 0 (Story 0.9) | @sm (River) |
| 2026-07-28 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-07-28 | 0.2.0 | Development started (YOLO mode) — Status: Ready → InProgress | @dev |
| 2026-07-28 | 0.3.0 | Development complete — Status: InProgress → InReview | @dev |
| 2026-07-28 | 0.4.0 | QA Gate CONCERNS — Status: InReview → Done | @qa |
| 2026-07-29 | 0.4.1 | Fidelidade visual (débito pós-story): substituídos ícones placeholder (emoji) por `@expo/vector-icons` (`Ionicons`) em `ProductForm` (câmera) e `SelectField` (chevron) — sem novo hex, cores 100% `darkColors`. `pnpm --filter @keepit/lojista typecheck` PASS. | @dev (Dex) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Dex, @dev) — YOLO mode.

### Debug Log References

Nenhum log de debug separado gerado. Validações executadas diretamente via CLI:
- `pnpm --filter @keepit/lojista typecheck` — PASS
- `pnpm turbo run typecheck` (todos os 9 workspaces) — PASS, sem regressão
- `pnpm turbo run lint` (todos os 9 workspaces) — PASS (scripts `echo skipped` em todo o monorepo, incluindo `@keepit/lojista` — nenhum linter real configurado ainda)
- CodeRabbit self-healing (Task de dev-develop-story) — **não executado**: ambiente é Linux nativo, sem `wsl` nem `coderabbit` no PATH (graceful degradation, ver `.claude/rules/coderabbit-integration.md`). Revisão de código automatizada fica pendente para quando a CLI estiver disponível (ou para o `@qa` rodar via `*qa-loop`).

### Completion Notes List

**Estado do repositório reconferido no início (Task 0):** ao contrário do que a seção "BLOCKER" dos Dev Notes descrevia (criada antes das Stories 0.2/0.3/0.8 existirem), no momento desta execução `packages/core-data` (Story 0.2) e a navegação real de `apps/lojista` (Story 0.3) e as telas de auth/perfil/pedidos (Story 0.8) **já estavam implementadas**. As 4 telas desta story (`GerenciarCatalogo`, `CadastrarProduto`, `EditarProduto`, `HorariosDisponibilidade`) já existiam como stubs (`createScreenStub`) registrados em `CatalogoStack.tsx`/`navigation/types.ts` — este trabalho substituiu os stubs pelo conteúdo real, sem tocar a estrutura de navegação existente (nomes de rota mantidos).

**Escopo de ambiente desta execução:** restrito a `apps/lojista/**` (+ 1 linha em `package.json` + symlink manual de `node_modules/@keepit/core-data`, sem rodar `pnpm install`). **Nenhum arquivo de `packages/core-data` foi criado/alterado.** Isso gerou 3 débitos técnicos documentados em código (comentários `[TECH DEBT]`) que precisam de uma story de reconciliação tocando `packages/core-data`:

1. **`store.port.ts` não tem método de escrita.** A Story 0.9 (Task 6) pedia `store.port.setPausedManually(...)` como correção da imprecisão do épico (toggle "Recebendo pedidos" pertence a `estabelecimentos.pausado_manualmente`, não a `produtos`) — mas `StorePort` (Story 0.2, como implementada) só expõe leitura (`getById`, `getState`, `listByHub`, `getCatalog`). Implementei o toggle **local-only** em `LojaDisponibilidadeContext.tsx` (lê o estado inicial via `store.port.getById`, mas a escrita não persiste no mock db). O domínio está correto (consumido via `store`, não `product`), só falta o write-path em `core-data`.
2. **`product.port.ts` não tem `delete()` nem forma de reativar (`update` sem campo `ativo`).** Só existe `pause()` (seta `ativo=false`). Implementei: pausar chama `product.port.pause(id)` de verdade (persiste no mock db); reativar e excluir são **local-only** no `ProductCatalogContext` (excluir chama `pause()` como melhor esforço antes de remover do array local). Documentado em comentário `[TECH DEBT]` no arquivo.
3. **`product.port.list()` filtra `ativo === true` incondicionalmente** — não há como buscar produtos pausados de volta do mock db. Por isso o catálogo é carregado **uma única vez** na montagem do `ProductCatalogProvider` e todas as mutações (pausar/reativar/excluir/criar/editar) atualizam só o array em memória a partir daí, nunca re-chamando `list()` — o que já era a orientação da Task 4 ("lista re-renderiza via estado em memória"), então não há regressão de comportamento nesta sessão, mas o provider ficaria inconsistente se fosse remontado (ex.: hot-reload) depois de uma pausa.

**Hooks/Context vivem em `apps/lojista`, não em `packages/core-data/src/hooks`** (onde `useHubs`/`useOrders`/`useStores`/`useWallet` vivem, por decisão da Story 0.2) — consequência direta da restrição de escopo acima. `[AUTO-DECISION]` + `[TECH DEBT]`: mover `ProductCatalogContext`/`LojaDisponibilidadeContext` para `@keepit/core-data/hooks` numa story de reconciliação, para manter a troca mock→Supabase (Épico 1) transparente às telas como as demais ports.

**Estoque (campo do protótipo, sem coluna real):** implementado como `estoqueDisplay` — hash determinístico do id do produto para fixtures existentes, e campo de formulário (`NumericStepper`) para produtos criados na sessão. Nunca enviado ao `ProductPort` (que não tem esse campo). Ver comentário em `types.ts`/`estoque.ts`.

**Outras `[AUTO-DECISION]` registradas em comentários de código:**
- Toggle pausar/reativar produto é **inline no card da lista** (não swipe/long-press) — o próprio protótipo (painel P7) já mostra o switch em cada linha.
- "Excluir produto" fica na tela **Editar produto** (não na lista), com `ConfirmModal`, mesmo padrão de `ExcluirConta.tsx` (Story 0.8).
- `GerenciarCatalogo` **sem seta de voltar** (é a rota raiz da tab Catálogo) — mesmo precedente de `Configuracoes.tsx` (Story 0.8).
- Upload de foto reaproveita o padrão mock de `PerfilPublico.tsx` (`Pressable` alternando `null`/`'local-placeholder'`) em vez de instalar `expo-image-picker` — dependência ausente em todo o monorepo hoje e instalá-la exigiria `pnpm install`, fora do permitido nesta execução.
- Campo "Categoria" é texto livre (`FormField`), não `SelectField`, para respeitar "categorias abertas (sem enum fixo)" dos Dev Notes.
- Loja "logada" fixa em `estab-farmacia-vida` (`currentStore.ts`) — sem autenticação real conectada ainda, mesma simplificação já usada por `lojaPerfilDraft.ts` (Story 0.8).
- Aviso de medicamento tarjado (Task 7) é só rótulo visual (constante `FARMACIA_TARJADO_AVISO`), sem bloqueio de submissão, conforme decisão já fechada em `docs/PERGUNTAS_REGRAS_NEGOCIO.md` (linha 357).

**Validação visual (AC3):** não há simulador iOS/emulador Android disponível neste ambiente (REQ-002, deferido) — fidelidade conferida por comparação estática entre o código e `docs/design-refs/lojista-07-catalogo.png` / `lojista-09-horarios.png` (cores/textos/tokens revisados manualmente linha a linha). @qa deve validar visualmente em simulador/emulador antes de aprovar o gate.

### File List

**Criados:**
- `apps/lojista/src/screens/catalogo/currentStore.ts`
- `apps/lojista/src/screens/catalogo/types.ts`
- `apps/lojista/src/screens/catalogo/estoque.ts`
- `apps/lojista/src/screens/catalogo/ProductCatalogContext.tsx`
- `apps/lojista/src/screens/catalogo/LojaDisponibilidadeContext.tsx`
- `apps/lojista/src/screens/catalogo/ProductForm.tsx`
- `apps/lojista/src/components/ScreenHeader.tsx`
- `apps/lojista/src/components/ToggleSwitch.tsx`
- `apps/lojista/src/components/ProductCard.tsx`

**Modificados:**
- `apps/lojista/src/screens/catalogo/GerenciarCatalogo.tsx` (stub → tela real, Task 1)
- `apps/lojista/src/screens/catalogo/CadastrarProduto.tsx` (stub → tela real, Task 2)
- `apps/lojista/src/screens/catalogo/EditarProduto.tsx` (stub → tela real, Task 3/4)
- `apps/lojista/src/screens/catalogo/HorariosDisponibilidade.tsx` (stub → tela real, Task 5/6)
- `apps/lojista/src/navigation/CatalogoStack.tsx` (envolve com `ProductCatalogProvider`/`LojaDisponibilidadeProvider`)
- `apps/lojista/src/navigation/types.ts` (`EditarProduto` ganha param `produtoId: string`)
- `apps/lojista/package.json` (+ dependência `@keepit/core-data: workspace:*`)

**Ambiente (fora do versionamento normal de código, mas necessário para o typecheck funcionar):**
- `apps/lojista/node_modules/@keepit/core-data` — symlink manual para `../../../packages/core-data` (mesmo padrão de `apps/admin`/`apps/cliente`). Débito de wiring a reconciliar com `pnpm install` real quando for seguro rodar no monorepo compartilhado.

## QA Results

### Review Date: 2026-07-28

### Reviewed By: Quinn (Test Architect)

### Verdict: CONCERNS (não-bloqueante)

Casca visual da trilha lojista completa, fiel ao protótipo (painéis P7/P9) e com tema dark
100% via tokens. As ressalvas são gaps reais de contrato em `packages/core-data` (fora do escopo
desta execução) que tornam alguns write-paths session-local — aceitáveis para o Épico 0, com
follow-up ARCH obrigatório antes do Épico 1.

### 7 Quality Checks

1. **Code review — PASS.** Código limpo, bem comentado, segue o padrão de context/provider da
   Story 0.8 (`LojaPerfilProvider`). Componentes novos (`ScreenHeader`, `ToggleSwitch`,
   `ProductCard`) reutilizados corretamente entre as 4 telas. `[AUTO-DECISION]`s razoáveis e
   documentadas (categoria texto livre, upload mock reusando padrão de `PerfilPublico`, excluir na
   tela Editar, toggle inline no card). Desvio menor documentado: `EditarProduto` pré-preenche via
   `context.getById` (estado local) em vez de `product.port.getById` — decisão correta dado que a
   port lê do mock db e divergiria do estado em memória.
2. **Unit tests — CONCERNS (não-bloqueante).** Nenhum teste adicionado para helpers puros
   testáveis (`agruparHorarios`, `estoqueDisplayFromId`). Consistente com o precedente de validação
   manual das 0.1/0.8 (monorepo sem harness de UI; lint = `echo`). Registrado como TEST-001.
3. **Acceptance criteria — PASS (AC1, AC3) / CONCERNS (AC2).**
   - AC1 PASS: as 6 telas/funcionalidades presentes (Gerenciar, Cadastrar, Editar, Pausar+Excluir,
     Horários, Toggle recebendo), stubs da 0.3 substituídos, rotas de `CatalogoStackParamList`
     preservadas (só `EditarProduto` ganhou param `produtoId`, aditivo).
   - AC2 CONCERNS: domínios corretos — produto via `product.port`, toggle via domínio `store`
     (`pausado_manualmente`), correção correta da imprecisão do épico. `create`/`update`/`pause`
     persistem no mock; reativar/excluir/toggle-loja são session-local por gaps de port
     (ARCH-001/002). Aceitável para a casca do Épico 0.
   - AC3 PASS: comparado item-a-item com `docs/design-refs/lojista-07-catalogo.png` e
     `lojista-09-horarios.png` — cores, textos ("Catálogo", "+ Novo", "Buscar produto",
     "Todos · N", "Loja aberta", "Recebendo pedidos agora", "Pausar novos pedidos",
     "FUNCIONAMENTO", "Salvar horários"), toggle animado e layout fiéis.
4. **No regressions — PASS.** `pnpm --filter @keepit/lojista typecheck` → PASS (exit 0),
   confirmado de forma independente. Escopo confinado a `apps/lojista/**` + 1 linha de dep em
   `package.json` + symlink de `node_modules/@keepit/core-data`; nenhum arquivo de
   `packages/core-data` alterado.
5. **Performance — PASS.** Casca visual; catálogo carregado uma única vez, mutações em memória.
6. **Security — PASS.** Sem rede/backend real, sem segredos, upload 100% mock/local. OWASP básico
   N/A a esta camada.
7. **Documentation — PASS.** File List completa e precisa; tech debt documentado em comentários
   `[TECH DEBT]`/`[AUTO-DECISION]` no código e no Dev Agent Record.

### Verificações independentes

- **Hex hardcoded:** grep nas telas/componentes novos → **limpo** (todas as cores via
  `@keepit/ui-tokens`). Nota: literal `#1B1E1C` permanece em `PrimaryButton.tsx` e
  `Configuracoes.tsx` — arquivos da Story 0.8, fora do File List desta story, não modificados aqui;
  pertence ao gate 0.8 (já CONCERNS), low, não bloqueia a 0.9.
- **Gaps de port confirmados no código:** `ProductPort` sem `delete()`, `UpdateProdutoInput` sem
  `ativo`, `pause()` como único write; `product.mock.list()` filtra `ativo === true`
  incondicionalmente; `StorePort` só leitura. Todos batem com o que o @dev documentou.
- **Fixture:** `estab-farmacia-vida` = `categoria: 'farmacia'` → aviso de tarjado (Task 7) dispara.

### Follow-ups (ARCH — para story de reconciliação antes do Épico 1)

- **ARCH-001 (medium):** estender `ProductPort` em `packages/core-data` — `delete()`/`excluido_em`,
  permitir `ativo` no `update` (ou `reactivate()`), e `list()` que inclua inativos. Hoje
  reativar/excluir/listar-pausados são local-only.
- **ARCH-002 (medium):** adicionar write em `StorePort`/`store.mock` (ex.: `setPausadoManualmente`)
  para o toggle de disponibilidade persistir.
- **MNT-001 (low):** mover `ProductCatalogContext`/`LojaDisponibilidadeContext` para
  `@keepit/core-data/hooks`.
- Consolidar estes com os gaps já achados nas 0.12/0.13.

### Gate Status

Gate: CONCERNS → docs/qa/gates/0.9-lojista-catalogo-disponibilidade.yaml
