# Story 12.1: Infraestrutura de mock Cliente persistente e versionado

## Status

Ready for Review

## Classificação

- Tipo: Arquitetura / Integração
- Complexidade: Alta
- Escopo: `apps/cliente` e `packages/core-data`; o mock Lojista permanece independente.

## Dependencies

- Nenhuma story do Épico 12.

## Story

**Como** pessoa testadora do app Cliente,  
**quero** que o cenário mock comece zerado, persista entre reinícios e possa ser restaurado,  
**para** construir e repetir livremente os fluxos do beta sem depender do app Lojista ou de backend.

## Acceptance Criteria

1. A primeira inicialização cria conta demo ativa, mas zero pedidos, favoritos de hubs/lojas, hub selecionado e itens no carrinho.
2. Conta/credenciais, perfil/senha, exclusão, pedidos/timestamps/status, favoritos, hub, carrinho e relógio/configuração QA persistem após encerrar e reabrir o app.
3. O armazenamento possui versão de schema e migração testada; estado incompatível é resetado de forma explícita e segura, sem crash ou estado parcial.
4. “Resetar cenário” exige confirmação, informa os dados afetados e restaura somente a fixture-base zerada.
5. Telas e hooks continuam consumindo ports de `@keepit/core-data`; nenhum fluxo de produto acessa diretamente o adapter ou AsyncStorage.
6. O mock Cliente não lê, grava nem sincroniza o estado do mock Lojista.
7. Testes cobrem hidratação, persistência, migração, reset, falha de armazenamento e isolamento Cliente/Lojista.

## Tasks / Subtasks

- [x] Definir o schema versionado e a fixture-base do mock Cliente (AC: 1, 2, 3, 6).
  - [x] Mapear o estado já mantido em memória/AsyncStorage e definir migrações aditivas.
  - [x] Garantir namespaces/chaves exclusivos do Cliente.
- [x] Implementar a fronteira de hidratação e persistência atrás dos ports (AC: 2, 5).
  - [x] Tratar gravação atômica, erro e reabertura.
- [x] Implementar reset confirmado e reconstrução da fixture-base (AC: 4).
- [x] Criar testes do adapter e armazenamento (AC: 1–7).

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["adapter tests", "persistence tests", "migration/reset review"]

## Dev Notes

- Fonte normativa: `docs/superpowers/specs/2026-09-04-estabilizacao-beta-android-cliente-design.md#5-mock-cliente-persistente`.
- O mock atual está concentrado em `packages/core-data/src/mock/db.ts`, fixtures em `packages/core-data/src/mock/fixtures/` e bootstrap em `apps/cliente/src/lib/dataClientBootstrap.ts`.
- Reutilizar o padrão de `apps/cliente/src/lib/cartStorage.ts`; não acoplar telas a uma tecnologia de persistência.
- Compatibilidade: ports existentes devem permanecer compatíveis ou receber extensões aditivas.
- Não semear pedidos, favoritos ou hub. “Zerado” não significa ausência da conta demo necessária ao login.

### Testing

- Vitest junto aos módulos alterados; testar instalação limpa, atualização de schema e reinício simulado.
- Verificar manualmente em build QA que o estado sobrevive a encerramento forçado e reset retorna ao baseline.

## 🤖 CodeRabbit Integration

- **Primary Type:** Architecture; **Secondary:** Integration; **Complexity:** High.
- **Primary Agents:** `@dev`, `@architect`; **Supporting:** `@qa`.
- [ ] Pre-Commit (`@dev`): contratos, atomicidade, migrações e testes.
- [ ] Pre-PR (`@github-devops`): retrocompatibilidade e isolamento do Lojista.
- **Self-Healing:** `@dev`, light, 2 iterações/15 min, auto-fix apenas CRITICAL; HIGH documentado.
- **Focus Areas:** corrupção/estado parcial, chaves isoladas, ausência de acesso direto pela UI e reset seguro.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---:|---|---|
| 2026-09-04 | 0.1 | Draft inicial derivado do Épico 12 e design aprovado. | @sm (River) |
| 2026-09-05 | 1.0 | Implementação, fixes e gates técnicos consolidados; smoke Android centralizado na Story 12.14. | @po (Pax) / @qa (Quinn) |

## QA Results

### Atualização final — 2026-09-05

**Gate técnico: PASS / Ready for Review.** Schema V7, hidratação, migrações,
persistência serializada, rollback, reset coordenado e isolamento Cliente/Lojista
estão implementados. O relatório final está em
`.superpowers/sdd/2026-09-04-mock-cliente-persistente/final-fix-report.md`; o
gate transversal posterior passou com `@keepit/core-data` 671/671 testes,
`@keepit/cliente` 356/356 e ambos os typechecks.

O Painel QA e sua confirmação de reset foram entregues pela Story 12.2. Um APK
QA mock também foi gerado e identificado, mas **não foi instalado nem observado
em Android**; persistência após encerramento forçado e reset visual continuam no
smoke único da Story 12.14. O registro abaixo é histórico e foi superado quanto
à disponibilidade do painel/APK, não quanto ao smoke manual.

### Registro histórico anterior à consolidação

Verificações integradas em 2026-09-04:

- `pnpm --filter @keepit/core-data test` — 29 arquivos, 550 testes passaram.
- `pnpm --filter @keepit/cliente test` — 23 arquivos, 172 testes passaram.
- `pnpm --filter @keepit/core-data typecheck` — `tsc --noEmit` passou.
- `pnpm --filter @keepit/cliente typecheck` — `tsc --noEmit` passou.

Pendências registradas naquele snapshot:

- A confirmação e a copy visual ainda aguardavam o Painel QA da Story 12.2; foram entregues posteriormente.
- O teste manual em APK release ainda dependia de um build QA instalável; o build existe agora, mas o smoke continua pendente.

A pendência manual foi transferida ao gate consolidado da Story 12.14.
