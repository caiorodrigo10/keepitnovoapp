# Story 11.2: Padronizar ações, campos e estados nas telas do Admin

## Status

Done

## MVP Pilot Classification

- Classificação: UI_ONLY
- Fonte: `docs/prd/epics/11-experiencia-painel-admin.md`
- Backend esperado: N/A — mantém dados mock e contratos existentes.
- Retomada futura: N/A

## Dependencies

- Story 11.1 concluída.
- Telas existentes em `apps/admin/app/(dashboard)/` e componentes em
  `apps/admin/src/components/`.

## Story

**Como** pessoa operadora,
**quero** encontrar os mesmos padrões visuais em filtros, formulários, CTAs e
estados do painel,
**para** operar diferentes áreas sem reaprender controles.

## Acceptance Criteria

1. Campos, selects e CTAs remanescentes das telas de aprovações, hubs, pedidos,
   clientes, lojistas, reembolsos, financeiro, qualidade e saques usam raio `md`,
   área mínima de 44 px e estados hover/focus coerentes.
2. Links de ação repetidos adotam o componente `Button` ou variante equivalente
   sem alterar rota, handler ou semântica.
3. Estados loading, vazio e erro preservam mensagens e ganham hierarquia visual
   consistente com `Card`/`Badge`.
4. As páginas mantêm leitura em 320 px e desktop, sem overflow horizontal e com
   navegação por teclado utilizável.
5. `pnpm --filter @keepit/admin typecheck` passa e os fluxos de dados mock não
   sofrem regressão.

## Tasks / Subtasks

- [x] Inventariar e consolidar classes de controle repetidas.
- [x] Criar/reutilizar primitivas de campo e ação conforme necessário.
- [x] Migrar por página, priorizando Aprovações, Hubs e Pedidos.
- [x] Fazer verificação visual, teclado e responsividade.

## Executor Assignment

executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: ["pnpm --filter @keepit/admin typecheck", "WCAG AA keyboard/focus check", "responsive manual check"]

## Dev Notes

- Não alterar `AdminPort`, hooks, fixtures, sessão ou rotas.
- Preferir tokens e componentes existentes; manter o escopo incremental, sem
  reestruturação extensa das páginas.

## 🤖 CodeRabbit Integration

- Tipo: Frontend; complexidade: média.
- Pre-commit (`@dev`): checar semântica, foco, contraste e repetição de classes.
- Pre-PR (`@github-devops`): validar comportamento das páginas e ausência de
  regressão de navegação.

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Dex, @dev) — YOLO mode.

### Debug Log References

Nenhum log de debug adicional gerado — sem falhas de build/typecheck durante a
execução.

### Completion Notes List

1. **Inventário (subtask 1)**: `grep -rn "rounded-sm" apps/admin/app/(dashboard)/`
   confirmou exatamente as 16 ocorrências em 13 arquivos de página já
   validadas pelo @po (Change Log 1.1.0) — 9 telas: aprovações, hubs,
   pedidos, clientes, lojistas, reembolsos, financeiro, qualidade-lojista e
   saques.
2. **Primitivas (subtask 2)**: `Button.tsx` exporta agora `BUTTON_BASE_CLASSES`/
   `BUTTON_VARIANT_CLASSES` (sem alterar o HTML/classes renderizadas por
   `Button`, só extraiu para reuso). Criado `LinkButton.tsx` — [IDS] ADAPT:
   reaproveita esses exports para estilizar `next/link` como CTA (AC2),
   evitando duplicar a paleta de cores/hover em 6 telas que usavam `<Link>`
   com classes soltas (`rounded-sm`, `hover:opacity-90` divergente do
   `hover:brightness-105` do `Button`). Nenhum componente novo de campo foi
   necessário — os inputs/selects/textareas remanescentes já seguiam o
   padrão de classes do `Button`/login (Story 11.1), só precisavam do
   raio/altura/transição corretos.
3. **Migração por página (subtask 3)**: aplicado em todas as 9 áreas da
   AC1 + `HubForm.tsx` (ver AUTO-DECISION abaixo). Padrão aplicado:
   - `rounded-sm` → `rounded-md` em todos os campos/CTAs remanescentes.
   - Inputs/selects de uma linha (busca de clientes, filtro de pedidos,
     seletor de lojista) ganharam `min-h-11` (44px) + `transition-colors`.
     Textareas (motivo de rejeição/suspensão/bloqueio/cancelamento, detalhe
     de erro) só ganharam `rounded-md`/`transition-colors` — já excedem 44px
     de altura por `rows={2,3}`, forçar `min-h-11` não altera nada.
   - 9 botões "Tentar novamente" (`<button>` cru, um por área da AC1) viraram
     `<Button variant="secondary" onClick={refresh}>` — AC2 ("links de ação
     repetidos"), ganhando raio/altura/foco consistentes com o resto do
     Admin em vez do estilo de link sublinhado solto.
   - 6 CTAs de navegação (`<Link>` estilizado à mão) viraram `<LinkButton>`:
     "+ Novo Hub"/"Processar" (reembolsos, saques) → `variant="primary"`;
     "Editar" (hubs)/"Ver detalhe" (lojistas, pedidos) → `variant="secondary"`.
     Rota, `href` e ausência de handler preservados — só a apresentação
     mudou (AC2: "sem alterar rota, handler ou semântica").
   - Segmented control de período no Financeiro (3 `<button>` com classe
     condicional manual) virou `<Button variant={ativo ? 'primary' :
     'secondary'}>` — mesmo padrão de toggle, sem lógica nova.
   - **[AUTO-DECISION]** `apps/admin/src/components/HubForm.tsx` (9
     ocorrências de `rounded-sm`, fora da contagem de "13 arquivos de
     página" do @po porque vive em `src/components/`, não em
     `app/(dashboard)/`) → incluído no escopo mesmo assim, porque é a
     superfície real de campos das telas `/hubs/novo` e `/hubs/[id]`
     (ambas listadas em AC1 sob "hubs"); deixá-lo intocado violaria o
     espírito da AC1 para essa área apesar de escapar do grep literal
     usado no inventário. Aplicado: `rounded-md` + `min-h-11` nos 5 campos
     de texto/número autônomos (nome, endereço, ponto de referência,
     lat, lng); `rounded-md` sem `min-h-11` nos 2 inputs de horário
     (`type="time"`, compactos, repetidos 7×2) e no preview da foto — forçar
     44px ali exigiria reestruturar a grade semanal, fora do "escopo
     incremental" pedido pelos Dev Notes; esses 2 inputs de horário também
     ganharam `focus:border-accent-brand focus:outline-none`, que não
     existia antes (nenhum estado de foco visível).
4. **Estados loading/vazio/erro (AC3)**: nenhuma mudança necessária — as 9
   telas já seguiam o padrão `Card` (erro com `border-accent-warning/40`,
   vazio com `Card` simples, loading como texto simples) desde antes desta
   Story; só o botão de retry dentro do card de erro mudou de estilo
   (item 3 acima). Mensagens preservadas literalmente.
5. **Verificação (subtask 4)**: `pnpm --filter @keepit/admin typecheck`
   limpo. Servidor local (`next dev -p 3113`, modo mock/`DATA_SOURCE`
   ausente) validado via `curl`: `/` → 307, `/login` → 200, e as 10 rotas de
   `(dashboard)` alteradas (incluindo `/hubs/novo`) → 200, sem
   erro/warning de hidratação no log do dev server. Sem browser MCP
   disponível neste ambiente para screenshot; validação de teclado feita
   por leitura do JSX — nenhum `tabIndex`/`outline-none` sem substituto foi
   introduzido, `Button`/`LinkButton` herdam o `:focus-visible` global
   (`globals.css`, Story 11.1) e os campos mantêm `focus:border-accent-brand
   focus:outline-none` já validado na Story 11.1. Responsividade: nenhuma
   classe de largura fixa/`overflow-x` adicionada; `min-h-11`/`rounded-md`
   não afetam layout em 320px.
6. **CodeRabbit**: `~/.local/bin/coderabbit` presente mas `signed out`
   (`coderabbit auth status`) neste ambiente — self-healing pulado com
   graceful degradation, conforme `dev-develop-story.md#Error Handling`.
   Recomendado ao `@ux-design-expert` (quality gate desta Story) rodar
   CodeRabbit se disponível no ambiente de review.
7. **Regressão de dados mock (AC5)**: nenhum hook, fixture ou `AdminPort`
   foi tocado — só classes CSS e composição de componentes de apresentação
   (`Button`/`LinkButton`). `refresh`/`onClick`/`href` de cada elemento
   convertido preservados literalmente.

### File List

**Criados:**
- `apps/admin/src/components/LinkButton.tsx`

**Modificados:**
- `apps/admin/src/components/Button.tsx` (exporta `BUTTON_BASE_CLASSES`/`BUTTON_VARIANT_CLASSES` para reuso; nenhuma classe renderizada mudou)
- `apps/admin/src/components/HubForm.tsx` (AUTO-DECISION, ver Completion Notes #3)
- `apps/admin/app/(dashboard)/aprovacoes/page.tsx`
- `apps/admin/app/(dashboard)/aprovacoes/[id]/page.tsx`
- `apps/admin/app/(dashboard)/hubs/page.tsx`
- `apps/admin/app/(dashboard)/lojistas/page.tsx`
- `apps/admin/app/(dashboard)/lojistas/[id]/page.tsx`
- `apps/admin/app/(dashboard)/pedidos/page.tsx`
- `apps/admin/app/(dashboard)/pedidos/[id]/page.tsx`
- `apps/admin/app/(dashboard)/clientes/page.tsx`
- `apps/admin/app/(dashboard)/reembolsos/page.tsx`
- `apps/admin/app/(dashboard)/reembolsos/[id]/page.tsx`
- `apps/admin/app/(dashboard)/saques/page.tsx`
- `apps/admin/app/(dashboard)/saques/[id]/page.tsx`
- `apps/admin/app/(dashboard)/qualidade-lojista/page.tsx`
- `apps/admin/app/(dashboard)/financeiro/page.tsx`

## QA Results

### Quality Gate — @ux-design-expert (Uma)

**Data:** 2026-08-14
**Veredito:** ✅ **PASS**
**Modo:** verificação independente (não confiei apenas no relatório do @dev)

#### Verificações executadas

| # | Verificação | Resultado |
|---|-------------|-----------|
| 1 | `grep -rn "rounded-sm"` em código-fonte (excluindo `.next/`) | ✅ 0 ocorrências reais — só 1 comentário histórico em `LinkButton.tsx:8` |
| 2 | `pnpm --filter @keepit/admin typecheck` (rodado por mim) | ✅ `tsc --noEmit` limpo |
| 3 | Escopo dos arquivos tocados (`git show --name-only`) | ✅ 16 arquivos de apresentação (pages/components) + story; nenhum hook/fixture/`adminClient`/`AdminPort`/session/rota/middleware |
| 4 | Dev server (`next dev -p 3114`) + smoke curl | ✅ `/`→307, `/login`→200, `/aprovacoes`→200, `/hubs`→200, `/hubs/novo`→200, `/pedidos`→200; sem erro de hidratação/runtime no log |
| 5 | HTML renderizado de `/hubs/novo` (HubForm) | ✅ 9× `rounded-md`, 0× `rounded-sm` |
| 6 | Classes de largura fixa/overflow em `/hubs` | ✅ nenhuma `w-[Npx]`/`min-w-[Npx]`/`overflow-x-scroll`; `shrink-0` preservado |
| 7 | Foco global (`globals.css:27`) | ✅ `:focus-visible { outline: 2px solid #75dc8d; outline-offset: 3px }` — indicador visível forte, herdado por `Button`/`LinkButton`/inputs |

#### Aderência aos Acceptance Criteria

- **AC1** ✅ Campos/selects/CTAs remanescentes das 9 áreas → `rounded-md` + `min-h-11` (44px) + `transition-colors` + hover/focus coerentes. Observação (não bloqueante): os 2 inputs `type="time"` da grade semanal do `HubForm` receberam `rounded-md` + estado de foco novo (`focus:border-accent-brand`, que não existia antes), mas **não** `min-h-11` — decisão documentada do @dev, justificada pelo "escopo incremental" dos Dev Notes (forçar 44px exigiria reestruturar a grade 7×2). `HubForm` estava fora do grep literal do @po (vive em `src/components/`), então a superfície AC1 validada pelo @po está 100% coberta; os time inputs são escopo-bônus com trade-off aceitável.
- **AC2** ✅ `LinkButton` novo (renderiza `<a href>` — semântica de link preservada) reaproveita `BUTTON_BASE_CLASSES`/`BUTTON_VARIANT_CLASSES` de `Button.tsx`. 6 CTAs de navegação e 9 botões "Tentar novamente" migrados; segmented control do Financeiro → `Button`. Rota/`href`/handler preservados literalmente.
- **AC3** ✅ Estados loading/vazio/erro mantêm mensagens literais e hierarquia `Card`/`Badge`; só o botão de retry mudou de estilo.
- **AC4** ✅ Foco visível global aplica a botões/links/inputs (todos nativamente focáveis); sem larguras fixas/overflow introduzidos; `/hubs` renderiza sem classes de overflow.
- **AC5** ✅ typecheck limpo (rodado por mim); fluxos mock intactos — nenhum hook/fixture/`AdminPort` tocado; páginas servem 200 sem regressão.

#### Dev Notes
✅ Respeitados — nenhuma alteração em `AdminPort`, hooks, fixtures, sessão ou rotas; escopo incremental sem reestruturação de páginas.

#### CodeRabbit
Pulado — `~/.local/bin/coderabbit` indisponível/signed-out neste ambiente (mesma graceful degradation reportada pelo @dev). Não bloqueia o gate.

**Decisão:** PASS. Story movida InReview → Done. Sem push (delegado a @devops).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-13 | 1.1.0 | Escopo da AC1 estendido para incluir telas de saques (rounded-sm remanescentes confirmados) | @po |
| 2026-08-13 | 1.1.0 | Validated GO (8/10) — Status: Draft → Ready | @po |
| 2026-08-13 | 1.2.0 | Início da implementação — Status: Ready → InProgress | @dev (Dex) |
| 2026-08-13 | 1.3.0 | Implementação completa: 16 ocorrências de `rounded-sm` (13 arquivos de página) + 9 em `HubForm.tsx` (AUTO-DECISION) eliminadas; `LinkButton` novo componente; `Button` exporta classes para reuso; `pnpm --filter @keepit/admin typecheck` OK. Status: InProgress → InReview | @dev (Dex) |
| 2026-08-14 | 1.4.0 | Quality gate PASS (verificação independente: 0 `rounded-sm` em código, typecheck limpo, 5 ACs atendidos, smoke curl OK, foco global validado, Dev Notes respeitados). Status: InReview → Done | @ux-design-expert (Uma) |
