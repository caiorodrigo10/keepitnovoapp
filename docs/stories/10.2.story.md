# Story 10.2: Dados de demonstração garantidos no Admin

## Status

Done

> Gate @qa: **PASS** (mudança aditiva, 5/5 AC). Verificação viva (orquestrador,
> 2026-07-31): tela de Aprovações do Admin lista **3 lojistas pendentes**
> (Padoca Nova + Mercado do Bairro + Pet Shop Amigo). core-data 107/107 verde.

## MVP Pilot Classification

- Classificação: SUPORTE (demo — garante que o Painel Admin não mostre telas
  vazias).
- Fonte: Épico 10 (`docs/prd/epics/10-admin-demo-mock.md`).
- Backend esperado: nenhum — enriquecimento **aditivo** de fixtures mock.

## Data Mode

- Modo: mock. Fixtures em `packages/core-data/src/mock/fixtures/`.
- Compatibilidade: mudanças aditivas; o app Cliente filtra lojas `ativo`, então
  novos `em_analise` não aparecem lá; Lojista/testes não devem regredir.

## Dependencies

- Independe da Story 10.1 (pode rodar antes ou depois). Toca `packages/core-data`
  (fixtures), enquanto a 10.1 toca só `apps/admin` — sem conflito de arquivos.

## Complexidade

SIMPLE — edição de fixtures + verificação visual. Sem lógica nova.

## Scope

**Incluído:** garantir dados representativos nas seções do Admin, priorizando as
que estão finas (Aprovações). Enriquecer fixtures onde necessário, mantendo
coerência com o schema (`Estabelecimento`, `ReembolsoPendente`,
`EstabelecimentoFalha`, etc.) e com o já existente.

**Excluído:** qualquer mudança de UI, novos campos de schema, mudança nos ports,
autenticação (Story 10.1).

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["pnpm --filter @keepit/core-data test", "pnpm qa (pacotes tocados)", "verificação manual no Admin: cada seção mostra dados"]

## Acceptance Criteria

1. **Aprovações** tem ≥ 2 estabelecimentos `status='em_analise'` nas fixtures
   (hoje só "Padoca Nova"). Adicionar 1–2 plausíveis (nome, categoria, endereço,
   horários — seguir o formato de `estabelecimentos.ts`).
2. **Lojistas** cobre `ativo`, `suspenso` (com `motivo_suspensao`) e
   `em_analise` — manter a variedade após o item 1.
3. **Reembolsos**, **Pedidos**, **Clientes** (≥1 bloqueado), **Qualidade
   Lojista** (falhas) e **Financeiro** (números não nulos) exibem dados.
   Enriquecer só onde estiver vazio/fino.
4. Mudanças aditivas; `pnpm --filter @keepit/core-data test` continua verde
   (ajustar asserts de contagem SOMENTE se algum teste fixar número exato de
   fixtures — documentar).
5. Sem regressão visível nos apps Cliente/Lojista.

## Tasks / Subtasks

- [x] **Task 1 — Aprovações mais cheias** (`fixtures/estabelecimentos.ts`)
  - [x] Adicionar 1–2 estabelecimentos `status='em_analise'` (ex.: "Mercado do
        Bairro" / "Pet Shop Amigo"), com `foto_fachada_url` (pode reusar o
        helper `foto(...)` já existente no arquivo), horários e endereço no
        mesmo padrão. `motivo_rejeicao/suspensao: null`, `pausado_manualmente:
        false`.
- [x] **Task 2 — Auditar demais seções**
  - [x] Conferir contagens atuais: `pedidos` (~28, ok), `clientes` (~10, tem
        bloqueado `cliente-bruno`), `reembolsos` (~3), `falhas` (~3), `hubs`
        (~3). Enriquecer só o que estiver vazio na tela correspondente.
  - [x] Financeiro: confirmar que `financialDashboard` retorna números não
        nulos com as fixtures (deriva de `pedidos`). Se vier zerado, ajustar
        fixtures de pedidos minimamente para gerar GMV/receita.
- [x] **Task 3 — Validação**
  - [x] `pnpm --filter @keepit/core-data test` verde (corrigir asserts de
        contagem exata se existirem).
  - [ ] Abrir cada seção do Admin e confirmar dados (ver quality_gate_tools) —
        **não executado por este agente** (restrição do orquestrador: não
        iniciar/reiniciar servidores de dev). Verificado via leitura das
        fixtures/portas (`AdminPort`) e contagens (ver Dev Agent Record).
        Deferido para @qa, que tem acesso ao Admin rodando via túnel.

## Dev Notes

- Fixtures ficam em `packages/core-data/src/mock/fixtures/` e são clonadas por
  `createMockDb()` (`mock/db.ts`). Elas alimentam TODOS os apps — manter
  aditivo.
- `AdminPort.pendingStores` filtra `status==='em_analise'`; `listAllEstabelecimentos`
  retorna todos; `financialDashboard(periodoDias)` deriva de `pedidos`.
- Cuidado com testes que fixam contagem exata (ex.: `store.mock.test.ts`,
  `admin.mock.test.ts`) — se algum assert `toHaveLength(n)` quebrar por causa de
  novos `em_analise`, atualizar o número e anotar no Dev Agent Record.
- Reusar o helper `foto()` já presente em `estabelecimentos.ts` para as fotos
  (imagens reais Unsplash já validadas em trabalho anterior).

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Dex, @dev) — modo YOLO (autônomo, spawnado pelo orquestrador).

### Decisões / Desvios

- **Aprovações (AC1):** adicionados 2 estabelecimentos `em_analise` em `packages/core-data/src/mock/fixtures/estabelecimentos.ts` —
  "Mercado do Bairro" (categoria `mercado`) e "Pet Shop Amigo" (categoria `pet`), seguindo exatamente o padrão de campos das
  fixtures existentes (mesmo `horarioPadrao()`, `motivo_rejeicao`/`motivo_suspensao: null`, `pausado_manualmente: false`). Fotos via
  helper `foto(...)` já existente, com novos IDs Unsplash verificados manualmente (`curl -o /dev/null -w '%{http_code}'` → `200`
  para ambos). Total de `em_analise` agora é 3 (Padoca Nova + as 2 novas) — atende "≥2" do AC1 com folga.
- **Categorias novas (`mercado`, `pet`):** `Estabelecimento.categoria` é `string` livre no `store.port.ts` (sem enum), e o app
  Cliente só mapeia rótulos de categoria para as telas de *discovery* que filtram `status==='ativo'` — como os 2 novos
  estabelecimentos são `em_analise`, nunca aparecem lá, então não há necessidade de adicionar rótulos novos em
  `discoveryDisplay.ts`/`StoreCard.tsx`. O Admin (`aprovacoes`/`lojistas`) renderiza `categoria` como texto cru, sem mapa de
  rótulos — nenhuma tela quebra com categorias novas.
- **AC2 (variedade de status em Lojistas):** já coberta antes desta story (`ativo`: Farmácia Vida, Bem Vestir, Conveniência 24h;
  `suspenso` com `motivo_suspensao`: Mercadinho Noturno; `em_analise`: agora 3 lojas) — nenhuma mudança adicional necessária, só
  confirmação.
- **AC3 (Reembolsos/Pedidos/Clientes/Qualidade Lojista/Financeiro):** auditoria confirmou que nenhuma seção estava vazia —
  `reembolsos` (3 fixtures), `falhas`/Qualidade Lojista (3), `clientes` (10, incluindo `cliente-bruno` com `bloqueado: true`),
  `pedidos` (23 fixtures, incluindo pedidos `entregue` com `entregue_em` dentro dos últimos 30 dias relativos à data corrente —
  ex.: `2026-07-10T14:36:00Z` e `2026-07-25T13:35:00Z`, ambos dentro da janela padrão de `financeiro/page.tsx`
  `periodoDias=30`). `financialDashboard(30)` deriva `gmvReais`/`receitaKeepitReais` > 0 a partir desses pedidos — **nenhuma
  fixture de pedidos foi alterada**, pois os números já não vêm zerados.
- **Nenhum teste de contagem exata quebrou** (`toHaveLength(n)` em `store.mock.test.ts`/`admin.mock.test.ts` etc.) — os 107 testes
  de `@keepit/core-data` seguiram verdes sem ajuste de asserts.
- Status da story foi movido diretamente para **Ready for Review** ao final (conforme instrução explícita do orquestrador que
  spawnou esta execução), pulando o `InReview` intermediário do fluxo padrão. @qa segue como próximo gate obrigatório.

### File List

**Modificados:**
- `packages/core-data/src/mock/fixtures/estabelecimentos.ts` (2 estabelecimentos `em_analise` adicionados, aditivo)

## QA Results

**Gate:** PASS
**Revisor:** Quinn (@qa, Opus) — 2026-07-31
**Escopo:** revisão estática das fixtures + comandos. Verificação visual de cada
seção do Admin deferida ao orquestrador — pendência de verificação, não falha.

### Comandos

| Comando | Resultado |
|---------|-----------|
| `pnpm --filter @keepit/core-data test` | **PASS** — 12 arquivos, **107 testes verdes** (nenhum assert de contagem exata quebrou) |
| `pnpm --filter @keepit/core-data typecheck` | **PASS** — `tsc --noEmit` limpo; valida que os 2 novos objetos conformam a `Estabelecimento` |

### Verificação AC-a-AC (estática)

| AC | Veredito | Evidência |
|----|----------|-----------|
| AC1 — ≥2 `em_analise` em Aprovações | ✅ | Agora **3**: `estab-em-analise` (Padoca Nova) + `estab-mercado-do-bairro` + `estab-pet-shop-amigo`. Todos com `motivo_rejeicao/suspensao: null`, `pausado_manualmente: false`, `horarios: horarioPadrao()`, foto via `foto(...)` — mesmo padrão das fixtures |
| AC2 — Lojistas cobre ativo/suspenso/em_analise | ✅ | `ativo`: farmacia-vida, bem-vestir, conveniencia-24h; `suspenso` com `motivo_suspensao`: mercadinho-noturno; `em_analise`: 3. Variedade preservada após AC1 |
| AC3 — Reembolsos/Pedidos/Clientes(bloqueado)/Qualidade/Financeiro com dados | ✅ | Spot-check QA: `clientes.ts` tem `bloqueado: true` (cliente-bruno); `reembolsos.ts` e `falhas.ts` populados; dashboard financeiro deriva de `pedidos` (dev confirmou pedidos `entregue` dentro da janela de 30d). Nenhuma seção vazia — nenhum enriquecimento adicional necessário |
| AC4 — aditivo, testes verdes | ✅ | 107 testes verdes, 0 asserts ajustados; única mudança é acréscimo de 2 objetos ao array |
| AC5 — sem regressão em Cliente/Lojista | ✅ | Cliente filtra `ativo` → novos `em_analise` nunca aparecem. Categorias novas (`mercado`/`pet`) são string livre; Admin renderiza cru; `CategoriaIcon` (Home.tsx) tem `default` fallback e opera sobre chips fixos de discovery, não sobre categoria crua de loja `em_analise` — nenhum caminho de quebra |

### Riscos avaliados (todos mitigados)

- **Categorias novas quebrando telas:** verificado que não há `switch`/`Record` exaustivo sobre `categoria` sem `default`. O único switch relevante (`apps/cliente/src/screens/home/Home.tsx:32`) tem `default` e nunca recebe lojas `em_analise`. OK.
- **Teste de contagem exata:** nenhum `toHaveLength(n)` quebrou — 107/107 verdes. OK.
- **Conformidade de schema:** typecheck de core-data limpo garante que os 2 objetos satisfazem `Estabelecimento` (sem campo faltante ou tipo errado). OK.

### Pendência de verificação viva (orquestrador — não é falha)

Abrir cada seção do Admin rodando e confirmar dados visíveis (Aprovações com 3 pendentes, Financeiro com números não nulos). Alta confiança via leitura de fixtures + testes verdes.

### Decisão

**PASS.** Mudança estritamente aditiva, todos os 5 AC atendidos, 107 testes verdes, typecheck limpo, sem caminho de regressão para Cliente/Lojista. Único item aberto é a conferência visual, deferida ao orquestrador.
