# Story 0.4: Cliente: Onboarding & Auth (visual)

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["typecheck", "lint", "manual visual QA (simulador iOS + emulador Android)"]

## Story

**As a** dev solo,
**I want** as telas de entrada do Cliente (onboarding, cadastro, confirmação de SMS, login, perfil e configurações) construídas com fidelidade visual ao protótipo e navegando sem autenticação real,
**so that** o fluxo de entrada do app Cliente exista ponta-a-ponta para revisão de UX antes de qualquer integração com Supabase Auth/Zenvia SMS (Épico 1+).

## Acceptance Criteria

1. Telas: Onboarding "Como funciona" (3 telas), Criar conta, Confirmação SMS, Login, Esqueci a senha, Perfil, Configurações + Excluir conta, modal de permissão push.
2. Fidelidade visual verificada contra o protótipo (cores, spacing, tipografia, textos).
3. "Login"/"Criar conta" navegam para a Home sem autenticação real; campos renderizados sem submit real.
4. Nenhum valor de regra hard-coded; qualquer texto de taxa/prazo vem de `packages/config`.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend
**Secondary Type(s)**: Integration (consumo da port `auth` de `packages/core-data`, ainda que mockada)
**Complexity**: Medium — 9 telas/estados de UI num único app (Cliente), sem lógica de servidor, mas com formulários (validação client-side sem submit real) e um fluxo de múltiplos passos (onboarding 3 telas + cadastro + confirmação SMS).

### Specialized Agent Assignment

**Primary Agents**:
- @dev (pré-commit, todas as stories)
- @ux-expert (fidelidade visual — telas de auth/perfil costumam ter muitos detalhes de estado: foco de campo, erro, disabled)

**Supporting Agents**:
- @architect (caso a forma de consumir a port `auth` mockada divirja do padrão estabelecido em `packages/core-data` pela Story 0.2)

### Quality Gate Tasks

- [x] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` antes de marcar a story completa (CodeRabbit CLI indisponível no sandbox — graceful degradation, ver Dev Agent Record)
- [ ] Pre-PR (@github-devops): Run `coderabbit --prompt-only --base main` antes de criar PR (quando o @devops for acionado)
- [ ] Pre-Deployment: Não aplicável — story não toca produção

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações, senão HALT manual)
- HIGH issues: auto_fix na 1ª iteração, document_as_debt se persistir na 2ª

### CodeRabbit Focus Areas

**Primary Focus**:
- Fidelidade visual: cores/spacing/tipografia batem com `packages/ui-tokens` (tema claro Cliente, `#F6F7F3`) — nenhum hex/tamanho hardcoded nas telas
- Nenhum valor de regra de negócio (taxa, prazo, dígitos de SMS, etc.) hardcoded na UI — tudo lido de `packages/config`

**Secondary Focus**:
- Formulários (Criar conta, Login, Esqueci a senha) não fazem submit real nem chamam rede — apenas navegação e validação client-side básica (campos obrigatórios, formato de e-mail)
- "Excluir conta" não implementa exclusão real — apenas o botão/CTA que, em produção, abrirá WhatsApp (FR24); nesta story o clique pode ser stub/no-op ou navegar para uma tela de confirmação visual, sem side-effect real

## Tasks / Subtasks

- [x] **Task 0 — Confirmar pré-requisitos antes de iniciar (bloqueante)**
  - [x] Confirmar que a Story 0.2 (`packages/core-data` — port `auth`, mocks assíncronos, `packages/config`) está **Done**. Esta story consome `auth.port.ts` (signIn, signUp, currentUser) e `packages/config` para qualquer texto de taxa/prazo/dígitos de SMS.
  - [x] Confirmar que a Story 0.3 (navegação `apps/cliente` com react-navigation — stack de auth) está **Done**. Esta story implementa as telas *dentro* do stack de auth já roteado pela 0.3; não deve criar sua própria solução de navegação.
  - [x] Se qualquer uma das duas não estiver Done, **HALT** e reportar ao @sm/@po — não inventar port `auth` nem estrutura de navegação para não divergir do que a 0.2/0.3 entregarem. (Ambas confirmadas `Done` — desbloqueado.)

- [x] **Task 1 — Onboarding "Como funciona" (3 telas) (AC: 1, 2, 3)**
  - [x] Implementar as 3 telas de onboarding conforme protótipo (`keepit-app/index.html`), com indicador de progresso (dots) e navegação avançar/pular
  - [x] Última tela do onboarding leva a Criar conta / Login (ou diretamente à Home, conforme roteamento definido pela Story 0.3 para o stack de auth)
  - [x] Sem chamada de rede, sem persistência de "onboarding já visto" além do que a 0.3 já provê como stub de guarda de navegação

- [x] **Task 2 — Criar conta (AC: 1, 2, 3, 4)**
  - [x] Formulário com campos: nome, e-mail, senha, telefone — conforme `[Source: docs/prd/02-requirements.md#FR1]` ("cadastro do cliente com nome, e-mail, senha e telefone")
  - [x] Aceite explícito de Termos de Uso e Política de Privacidade (checkbox, sem link funcional obrigatório nesta story — pode ser texto estático)
  - [x] CPF **não** aparece nesta tela — é coletado apenas no primeiro checkout (fora do escopo do Épico 0, ver Dev Notes / decisão de negócio)
  - [x] Botão "Criar conta" apenas navega (para a tela de Confirmação SMS, ou direto à Home conforme o stack da 0.3) — **sem submit real**, sem chamar `auth.port.signUp` de forma bloqueante (pode chamar a versão mock apenas para exercitar o estado de loading, se a UX do protótipo tiver esse estado)
  - [x] Nenhum texto de taxa/prazo hardcoded — se a tela exibir qualquer copy desse tipo, o valor vem de `packages/config` (esta tela não exibe copy de taxa/prazo — N/A)

- [x] **Task 3 — Confirmação SMS (AC: 1, 2, 3)**
  - [x] Tela de input de código (4-6 dígitos, conforme `[Source: docs/prd/02-requirements.md#FR2]` — "SMS via Zenvia com código de 4-6 dígitos") — nesta story é **apenas o teclado/input visual**, sem envio real de SMS (Zenvia é integração de backend, fora do Épico 0)
  - [x] Reenviar código / contagem regressiva pode ser um estado visual estático ou mock — sem chamada real
  - [x] Confirmar código apenas navega para a Home — sem validação de código real

- [x] **Task 4 — Login (AC: 1, 2, 3)**
  - [x] Formulário e-mail + senha, conforme `[Source: docs/prd/02-requirements.md#FR3]`
  - [x] Botão "Entrar" apenas navega para a Home — sem autenticação real, sem chamar `auth.port.signIn` de forma bloqueante
  - [x] Link "Esqueci minha senha" navega para a tela correspondente

- [x] **Task 5 — Esqueci a senha (AC: 1, 2, 3)**
  - [x] Formulário de e-mail para recuperação, conforme `[Source: docs/prd/02-requirements.md#FR3]` ("fluxo 'esqueci minha senha' via e-mail")
  - [x] Botão "Enviar" apenas navega (para uma tela de confirmação visual ou volta ao Login) — sem envio real de e-mail

- [x] **Task 6 — Perfil (AC: 1, 2)**
  - [x] Tela de Perfil com dados básicos editáveis (nome, e-mail, telefone), conforme `[Source: docs/prd/02-requirements.md#FR21]` — campos populados via port `auth`/mock de cliente (dado mock, não input do usuário anterior)
  - [x] Edição é apenas visual (campos ficam editáveis, "Salvar" navega/volta sem persistir de verdade) — sem chamada de rede

- [x] **Task 7 — Configurações + Excluir conta (AC: 1, 2)**
  - [x] Tela de Configurações incluindo toggle de notificações push (`notificacoes_ativas`, ver Dev Notes) — toggle é local/visual, sem persistência real
  - [x] Botão "Excluir minha conta" conforme `[Source: docs/prd/02-requirements.md#FR24]` — em produção abre WhatsApp da Keepit com mensagem pré-preenchida ("Quero excluir minha conta"); nesta story, como não há integração real com WhatsApp/deep-link obrigatória no Épico 0, implementar como CTA visualmente fiel ao protótipo que **navega para uma tela/estado de confirmação stub** (sem abrir WhatsApp de fato) — documentar a decisão no Dev Agent Record
  - [x] Botões "Falar com Keepit" / "Falar com o lojista" (se presentes nesta tela no protótipo) seguem o mesmo tratamento — CTA visual, sem deep-link funcional

- [x] **Task 8 — Modal de permissão push (AC: 1, 2, 3, 4)**
  - [x] Modal implementado como rota modal (conforme padrão de modais estabelecido pela Story 0.3 — AC4 da 0.3: "Modais (CPF, PIN, permissão push) implementados como rotas modais")
  - [x] Copy fiel ao protótipo, opt-in leve conforme `[Source: docs/prd/02-requirements.md#FR25]` ("habilitado por padrão após cadastro; opção de desligar em Configurações") — sem chamada real à API de push do SO nesta story (nenhuma dependência de rede/notificação real introduzida)
  - [x] Ação "Permitir"/"Agora não" apenas fecha o modal e navega — sem side-effect real

- [x] **Task 9 — Fidelidade visual e checklist (AC: 2, 4)**
  - [x] Conferir visualmente cada uma das 9 telas/estados desta story contra o protótipo `keepit-app/index.html` (cores, spacing, tipografia, textos) — usar tema claro `#F6F7F3` do Cliente (verificado via as 4 imagens de referência de `docs/design-refs/` + design system legend; débito de validação em device real documentado abaixo)
  - [x] Confirmar que nenhuma tela desta story tem hex/valor de regra hardcoded — todas as cores vêm de `@keepit/ui-tokens`, todo texto de taxa/prazo/dígitos vem de `packages/config` (grep confirmou zero hex fora de comentários)
  - [x] Rodar `pnpm turbo run typecheck` e `pnpm turbo run lint` sem regressão nos demais workspaces (adaptado para `pnpm --filter @keepit/cliente typecheck` — ver Dev Agent Record, restrição de ambiente paralelo)
  - [x] Atualizar File List da story com todos os arquivos criados/modificados

## Dev Notes


> **Correção (2026-07-28):** todas as stories 0.1–0.13 **já existem** como arquivos em `docs/stories/` (a 0.1 está `Done`; 0.2–0.13 estão `Ready`). Onde este Dev Notes disser que story files de 0.2/0.3/etc. "não existem" ou que "só a 0.1 existe", leia como **desatualizado**. O fato substantivo que permanece **verdadeiro** é: 0.2 (`core-data`/`config`) e 0.3 (navegação) ainda **não foram implementadas em código** — por isso a Task 0 / gate de dependência continua válida antes de `InProgress`.
### Previous Story Insights

- **Story 0.1** (Done, gate CONCERNS) — `apps/cliente` já está bootado com Expo, `useFonts` funcionando, Home mínima com paleta `#F6F7F3`. O sandbox de desenvolvimento **não tem simulador iOS/emulador Android/browser interativo disponível** — a validação visual empírica ficou como débito deferido (REQ-002) no gate de 0.1, e o mesmo padrão se aplica a esta story: a fidelidade visual (AC2) deve ser verificada por inspeção de código (import exclusivo de `@keepit/ui-tokens`, sem hex hardcoded) quando não houver ambiente visual disponível, com débito explícito documentado para validação posterior em device real.
- **Stories 0.2 e 0.3 ainda não têm arquivo de story criado** (`docs/stories/` só contém `0.1.story.md` no momento da criação desta story). Isso é uma **violação da ordem sequencial normal do processo SM** (Story Development Cycle exige stories concluídas em ordem) — esta story 0.4 foi solicitada fora de sequência. Documentado explicitamente como blocker: ver Task 0 acima. O @dev **não deve iniciar a implementação desta story antes de 0.2 e 0.3 estarem `Done`**, pois:
  - Sem a Story 0.2, não existe `packages/core-data` (porta `auth.port.ts`) nem `packages/config` no repositório — esta story depende de ambos para os ACs 3 e 4.
  - Sem a Story 0.3, não existe react-navigation configurado em `apps/cliente` (stack de auth) — esta story depende da estrutura de rotas já definida por ela para não inventar uma solução de navegação paralela.

### Estado atual verificado do repositório (ler antes de codar)

- `packages/core-data/` e `packages/config/` **não existem** no repositório no momento da criação desta story (`ls packages/` retorna apenas `shared-types`, `supabase-client`, `ui-tokens`). Confirma o blocker acima.
- `apps/cliente/` hoje contém apenas o boot mínimo da Story 0.1: `App.tsx`, `index.ts`, `app.json`, sem estrutura de telas/rotas ainda. `package.json` de `apps/cliente` tem apenas `@keepit/ui-tokens`, `expo`, `expo-font`, `expo-status-bar`, `react`, `react-native` como dependências — **sem `@react-navigation/*`** (dependência que a Story 0.3 deve introduzir).
- `packages/ui-tokens/src/tokens.json` já contém a paleta clara do Cliente (`colors.light`, fundo `#F6F7F3`) e tipografia Hanken Grotesk — consumir via `@keepit/ui-tokens`, nunca hardcodear hex.

### Escopo do Épico 0 (contexto obrigatório)

- Regra de ouro: "se a tela precisa *renderizar*, entra no Épico 0. Se precisa *persistir, autenticar, cobrar ou disparar side-effect real*, fica para depois." [Source: docs/prd/epics/0-casca-visual.md#Escopo — o que ENTRA e o que NÃO entra]
- **Explicitamente fora de escopo nesta story:**
  - Nenhuma chamada real a Supabase Auth, Zenvia SMS, ou qualquer backend.
  - Nenhuma validação de senha/e-mail no servidor; validação client-side básica (campo obrigatório, formato) é aceitável se o protótipo já mostrar esse feedback visual.
  - Nenhum deep-link real para WhatsApp na tela de Excluir conta (ver Task 7).
  - Nenhuma persistência real de "notificações ativas" ou de dados de perfil editados.

### Regras de negócio já decididas (fonte: stakeholder, via Caio)

`[Source: docs/PERGUNTAS_REGRAS_NEGOCIO.md#Decisões → Rodada 2 → Cadastro do cliente]`

- **Campos obrigatórios no cadastro**: e-mail + senha + telefone. **CPF é opcional no cadastro** (só se torna obrigatório no primeiro checkout — fora do escopo desta story/épico).
- **Confirmação de telefone**: SMS com código de **4-6 dígitos**. Serve também como fallback caso o cliente perca acesso ao app antes da retirada (contexto de produto, não implementado nesta story).
- **Login social**: fora do MVP. Só e-mail + senha. Apple/Google fica para v2 — **não construir essas telas/botões nesta story**.
- **Guest checkout**: não existe. Cadastro é obrigatório antes de comprar (não afeta diretamente esta story, mas reforça que "Criar conta" é fluxo central, não opcional).

### Requisitos funcionais relevantes (fonte: PRD)

- **FR1**: cadastro do cliente com nome, e-mail, senha e telefone, exigindo aceite explícito dos Termos de Uso e Política de Privacidade. [Source: docs/prd/02-requirements.md#FR1]
- **FR2**: SMS via Zenvia com código de 4-6 dígitos para confirmação de telefone logo após o cadastro (nesta story, apenas a UI do input de código). [Source: docs/prd/02-requirements.md#FR2]
- **FR3**: login do cliente com e-mail e senha, incluindo fluxo "esqueci minha senha" via e-mail. [Source: docs/prd/02-requirements.md#FR3]
- **FR4**: onboarding de 3 telas ("Como funciona") no primeiro uso, seguindo o protótipo fielmente. [Source: docs/prd/02-requirements.md#FR4]
- **FR21**: tela de "Perfil" com dados básicos, editáveis. [Source: docs/prd/02-requirements.md#FR21]
- **FR24**: botão "Excluir minha conta" no Perfil/Configurações que, em produção, abre WhatsApp da Keepit com mensagem pré-preenchida ("Quero excluir minha conta") — atende Apple Guideline 5.1.1(v) e LGPD. [Source: docs/prd/02-requirements.md#FR24]
- **FR25**: permissão de push notification solicitada de forma opt-in leve (habilitado por padrão após cadastro; opção de desligar em Configurações). [Source: docs/prd/02-requirements.md#FR25]
- **NFR9 / NFR10**: exclusão de conta acionável via WhatsApp é requisito de compliance (LGPD / Apple Guideline 5.1.1(v)) — reforça que o botão de Excluir conta deve existir e ser visualmente presente mesmo que o deep-link real fique fora do Épico 0. [Source: docs/prd/02-requirements.md#NFR9, #NFR10]

### Modelo de dados de referência (não implementado nesta story — apenas para os mocks refletirem os mesmos nomes de campo)

`clientes` (Supabase, a ser espelhada pelo mock de `packages/core-data` na Story 0.2): `id`, `nome`, `telefone`, `telefone_confirmado`, `cpf` (nullable), `expo_push_token`, `notificacoes_ativas` (default true), `bloqueado`, `motivo_bloqueio`, `hub_padrao_id` (reservado v2, NULL no MVP), `criado_em`, `atualizado_em`, `excluido_em`. [Source: docs/architecture/03-data-models.md#1.1 clientes]

- A tela de Perfil (Task 6) deve exibir campos consistentes com esse schema (nome, telefone), e a de Configurações (Task 7) o toggle de `notificacoes_ativas`. Isso é relevante para a Story 0.2 não precisar reformular o mock quando esta story ler os dados — nomes de campo devem coincidir.

### Fidelidade visual — paleta do Cliente

- Tema **claro**, fundo `#F6F7F3`, conforme `packages/ui-tokens/src/tokens.json` (`colors.light`). [Source: docs/prd/epics/0-casca-visual.md#Story 0.1 — AC4, reaproveitado como base de tema para todo o app Cliente]
- Nenhuma cor deve ser hardcodeada — sempre importar de `@keepit/ui-tokens`.

### Referência visual do protótipo (limitação conhecida)

- `keepit-app/index.html` (461 KB) é um bundle exportado (provavelmente de ferramenta tipo v0/Lovable) sem texto legível via grep simples (strings de UI aparentam estar embutidas em JS compilado/minificado, não em atributos HTML estáticos com `id`/`data-*` previsíveis). Tentativas de `grep` por marcadores como "Como funciona", `data-screen`, `id="onboarding"` não retornaram resultado.
- **Consequência para @dev**: a verificação de fidelidade visual (AC2) desta story não pode ser feita por leitura de texto do HTML — deve ser feita **visualmente** (abrir o arquivo num browser/DevTools, ou captura de tela, se o ambiente permitir) ou, na ausência de ambiente visual (mesma limitação de sandbox documentada na Story 0.1), por inspeção assistida da estrutura do bundle e documentação explícita do débito para @qa validar depois com as ferramentas reais.
- Nenhuma tela desta story deve reproposta a UX — a fidelidade é ao que está no protótipo, não a uma reinterpretação.

### File Locations (novo código desta story, dentro de `apps/cliente/`)

- Estrutura exata de pastas depende do que a Story 0.3 estabelecer para o stack de navegação do Cliente (react-navigation). Sugestão alinhada ao padrão comum Expo + react-navigation (a confirmar contra o que 0.3 efetivamente entregar): `apps/cliente/src/screens/onboarding/`, `apps/cliente/src/screens/auth/` (CriarConta, ConfirmacaoSms, Login, EsqueciSenha), `apps/cliente/src/screens/perfil/` (Perfil, Configuracoes), `apps/cliente/src/components/modals/` (PermissaoPush).
- Consumo de dados via hook/provider da port `auth` (`useAuth()` ou equivalente) — nome exato depende do que a Story 0.2 entregar; não inventar uma API diferente da que 0.2 definir.

### Testing

- Não há testes automatizados de UI nesta story (mesma decisão da Story 0.1 — validação é empírica/visual, alinhada ao princípio "backend simples, MVP é MVP" do CLAUDE.md).
- `pnpm turbo run typecheck` e `pnpm turbo run lint` devem seguir passando para todos os workspaces (sem regressão).
- Validação manual: percorrer o fluxo Onboarding → Criar conta → Confirmação SMS → Home, e separadamente Login → Home, e separadamente Esqueci a senha, Perfil, Configurações (com o botão Excluir conta), e o modal de permissão push — confirmando fidelidade visual e ausência de qualquer chamada de rede real (inspecionável por não haver nenhuma dependência de rede introduzida no código, já que a auth real só chega no Épico 1).
- Se a port `auth` mock (Story 0.2) tiver testes unitários próprios de contrato, esta story não precisa duplicá-los — apenas consumir a port já testada.
- [No specific guidance found in architecture docs for automated UI testing strategy neste estágio do projeto — decisão de manter validação manual está alinhada à Story 0.1]

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-27 | 0.1 | Story criada a partir do Épico 0 (Story 0.4) | @sm (River) |
| 2026-07-28 | 0.1.1 | Validated GO (10/10) — Status: Draft → Ready | @po |
| 2026-07-28 | 0.2.0 | Implementação completa (Tasks 0-9) — Status: Ready → InProgress → InReview | @dev (Dex) |
| 2026-07-28 | 0.3.0 | QA Gate CONCERNS — Status: InReview → Done | @qa (Quinn) |
| 2026-07-28 | 0.3.1 | Fix REQ-004 (débito de fidelidade do gate): onboarding do Cliente (Onboarding1/2/3 + `OnboardingScreen`/`OnboardingIllustration`) convertido para tema DARK, fiel a `cliente-01-onboarding.png` — fundo/wordmark/heading/subtext agora usam `darkColors`, `Dots` recebeu prop opcional `theme` (default `'light'` preservado, sem impacto nas demais telas) e `Screen` recebeu prop opcional `backgroundColor` (default inalterado). Resto do app Cliente permanece claro. 100% via `@keepit/ui-tokens`, zero hex hardcoded. `pnpm --filter @keepit/cliente typecheck` PASS. | @dev (Dex) |
| 2026-07-29 | 0.3.2 | Fix de fidelidade visual (débito revelado ao renderizar o app, story já `Done`): `HouseIcon.tsx` (ícone central do card de onboarding, dentro do círculo verde) trocado da versão desenhada com Views puras (triângulo via borda + retângulo) para `Ionicons name="home-outline"`, via `@expo/vector-icons` recém-instalado em `apps/cliente`. Mesma assinatura de props (`color`/`size`), sem mudança em `OnboardingIllustration.tsx`. `pnpm --filter @keepit/cliente typecheck` PASS. | @dev (Dex) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Sonnet mais recente, conforme regra fixa do CLAUDE.md para `@dev`)

### Debug Log References

Nenhum log de debug de execução foi necessário — implementação linear sem falhas de build bloqueantes além dos erros de `tsc` corrigidos iterativamente (ver Completion Notes, item "Typecheck"). Nenhuma entrada em `.ai/debug-log.md`.

### Completion Notes List

**Pré-requisitos (Task 0):** Confirmado que Story 0.2 (`packages/core-data`/`packages/config`) e Story 0.3 (navegação `apps/cliente`) estão `Done` — desbloqueado para iniciar.

**Kit de UI compartilhado (novo, [IDS] CREATE):** Não existia nenhum componente reutilizável em `apps/cliente` além do `ScreenStub` (Story 0.3). Criado `src/components/ui/` (`Screen`, `Button`, `TextField`, `Checkbox`, `Dots`, `BellIcon`) e `src/components/onboarding/` (`HouseIcon`, `OnboardingIllustration`, `OnboardingScreen`) para evitar duplicar o mesmo JSX/estilo nas 9 telas. Cada decisão de reuso está documentada como comentário `[IDS]` no topo do arquivo.

**Sem lib de ícones:** `apps/cliente/package.json` não tem `@expo/vector-icons` (nem nenhuma outra lib de ícones) instalada. Em vez de adicionar uma dependência nova (fora do escopo autorizado desta story — não rodar `pnpm install`), os 2 ícones necessários (casa do onboarding, sino do modal push) foram desenhados com `View`s puras (bordas/retângulos). Fidelidade aproximada, não pixel-perfect ao ícone do protótipo — documentado como débito de polish visual.

**Dependência faltante `@keepit/core-data` em `apps/cliente`:** a Story 0.3 não adicionou `@keepit/core-data` como dependência de `apps/cliente/package.json` (só `apps/admin` já tinha). Como esta story precisa consumir a port `auth`, adicionei `"@keepit/core-data": "workspace:*"` ao `package.json` e criei manualmente o symlink `apps/cliente/node_modules/@keepit/core-data → ../../../../packages/core-data` (mesmo padrão já existente em `apps/admin/node_modules/@keepit/`), **sem rodar `pnpm install`** (proibido pelo prompt de spawn) e **sem tocar `pnpm-lock.yaml`**. Isso é suficiente para `tsc --noEmit` resolver os tipos no sandbox; o lockfile real deveria ser regenerado por `@devops`/CI numa etapa normal de `pnpm install` fora deste sandbox restrito.

**[AUTO-DECISION] Login social (Google/Apple):** o protótipo (`cliente-09-criar-conta.png`, `cliente-10-login.png`) mostra os botões Google/Apple, mas a decisão de negócio da Rodada 2 (`docs/PERGUNTAS_REGRAS_NEGOCIO.md`) tira login social do MVP → **não implementados**. Documentado no cabeçalho de `CriarConta.tsx`/`Login.tsx`. Este é o desvio consciente do protótipo previsto no prompt de spawn desta story.

**[AUTO-DECISION] Campo Telefone em Criar conta:** a captura `cliente-09-criar-conta.png` só mostra Nome/E-mail/Senha, mas FR1 exige telefone no cadastro → adicionado um 4º campo (Telefone) não presente na captura, priorizando a exigência funcional (FR1) sobre a réplica 1:1 do recorte estático disponível (reason: sem o campo, a Confirmação SMS ficaria sem destino de envio).

**[AUTO-DECISION] `Cliente` (Story 0.2) não tem `email`/`senha`:** a port `AuthPort.signUp` só aceita `{ nome, telefone }` (auth do MVP é telefone + SMS, não e-mail/senha — ver docstring de `auth.port.ts`, que já antecipava isso). As telas Criar conta/Login continuam coletando e-mail/senha por fidelidade ao protótipo e ao FR1/FR3, mas **só nome+telefone são efetivamente passados ao mock** (`signUp`). Nenhum submit real ocorre, então não há perda de dado real — mas registro aqui como um gap entre PRD (FR1: "e-mail, senha") e o modelo de dados já `Done` da Story 0.2 (`Cliente` sem esses campos), a ser reconciliado com o stakeholder antes do Épico 1 (auth real). Recomendo ao @qa escalar esta observação.

**[AUTO-DECISION] Perfil sem sessão mock ativa no boot:** `authGuard.ts` (Story 0.3) sempre deixa passar direto para `MainTabs`, então `auth.currentUser()` normalmente retorna `null` ao abrir Perfil (nenhum `signIn`/`signUp` foi chamado ainda). Criado `useCurrentCliente()` (`src/hooks/useCurrentCliente.ts`) que, se não houver sessão, chama `auth.signIn()` com o telefone da única fixture do mock (`clientesFixture`, Story 0.2) — reaproveita o dado mock já existente em vez de inventar um novo. Perfil também usa `useOrders(clienteId)` (hook já existente da Story 0.2) para o card "Pedidos" mostrar contagem real; "Hubs favoritos" fica em 0 (não existe port de favoritos no Épico 0).

**[AUTO-DECISION] Perfil mostra telefone, não e-mail, como subtítulo:** `Cliente` não tem campo e-mail (ver acima) — subtítulo do avatar usa telefone, mantendo fidelidade estrutural ao protótipo (nome + linha secundária) sem inventar um e-mail fake.

**[AUTO-DECISION] Menu do Perfil:** dos 5 itens visíveis em `cliente-08-perfil.png` (Meus pedidos, Hubs favoritos, Formas de pagamento, Notificações, Ajuda & suporte), só "Meus pedidos" (→ tab Pedidos) e "Notificações" (→ tela Configurações, onde está o toggle push) foram conectados — os demais (Hubs favoritos, Formas de pagamento, Ajuda & suporte) não têm rota/port correspondente no inventário da Story 0.3/0.2, ficam visuais/desabilitados. Fora de escopo explícito da Task 6.

**Onboarding 1 e 2 sem captura dedicada:** `docs/design-refs/INDEX.md` só tem uma captura de onboarding (`cliente-01-onboarding.png`, mapeada para a tela final/3ª). As telas 1 e 2 foram reconstruídas a partir dos cards "Como funciona" da legenda (`_design-system-legend.png`: "1 · Compra", "2 · Pronto" — texto do 2º card cortado na captura) usando o mesmo layout/tokens da 3ª tela. Documentado como inferência assistida no cabeçalho de `Onboarding1.tsx`/`Onboarding2.tsx`, mesma limitação de referência já registrada nas Dev Notes da story.

**Excluir conta / notificações push:** `ExcluirConta.tsx` é um stub visual sem deep-link real para WhatsApp (conforme Task 7). Toggle de `Configuracoes.tsx` é local/visual — `Cliente` não expõe `notificacoes_ativas` na port (só no schema Supabase de referência), então não há dado real para ler/persistir.

**Validação visual empírica (débito deferido, REQ-002, mesmo padrão da Story 0.1):** o sandbox não tem simulador iOS/emulador Android disponível. Fidelidade visual (AC2) verificada por: (1) inspeção de código — 100% das cores/tipografia vêm de `@keepit/ui-tokens`, zero hex hardcoded (confirmado por grep); (2) comparação estrutural manual contra as 4 imagens de referência (`cliente-01-onboarding.png`, `cliente-09-criar-conta.png`, `cliente-10-login.png`, `cliente-08-perfil.png`) e a legenda de design system. Validação em device/simulador real fica como débito para o @qa.

**Typecheck:** `pnpm --filter @keepit/cliente typecheck` está verde. **Não rodei `pnpm turbo run typecheck`/`lint` globais** — o prompt de spawn desta story instrui explicitamente evitar `turbo run` global durante a paralelização com o @dev irmão trabalhando em `apps/lojista`/`apps/admin`, para não tocar workspaces fora do escopo desta story. `apps/cliente/package.json#lint` é `echo skipped` (stub desde a Story 0.1) — nada a rodar.

**CodeRabbit:** não executado — CLI não confirmada disponível neste sandbox (graceful degradation, conforme `coderabbit-integration.md`). Recomendo ao @qa rodar `coderabbit --prompt-only -t committed --base main` no ambiente real antes do gate final.

### File List

**Novos:**
- `apps/cliente/src/components/ui/Screen.tsx`
- `apps/cliente/src/components/ui/Button.tsx`
- `apps/cliente/src/components/ui/TextField.tsx`
- `apps/cliente/src/components/ui/Checkbox.tsx`
- `apps/cliente/src/components/ui/Dots.tsx`
- `apps/cliente/src/components/ui/BellIcon.tsx`
- `apps/cliente/src/components/ui/index.ts`
- `apps/cliente/src/components/onboarding/HouseIcon.tsx`
- `apps/cliente/src/components/onboarding/OnboardingIllustration.tsx`
- `apps/cliente/src/components/onboarding/OnboardingScreen.tsx`
- `apps/cliente/src/hooks/useCurrentCliente.ts`

**Modificados (stubs da Story 0.3 substituídos por telas reais):**
- `apps/cliente/src/screens/auth/Onboarding1.tsx`
- `apps/cliente/src/screens/auth/Onboarding2.tsx`
- `apps/cliente/src/screens/auth/Onboarding3.tsx`
- `apps/cliente/src/screens/auth/CriarConta.tsx`
- `apps/cliente/src/screens/auth/ConfirmacaoSMS.tsx`
- `apps/cliente/src/screens/auth/Login.tsx`
- `apps/cliente/src/screens/auth/EsqueciSenha.tsx`
- `apps/cliente/src/screens/perfil/Perfil.tsx`
- `apps/cliente/src/screens/perfil/Configuracoes.tsx`
- `apps/cliente/src/screens/perfil/ExcluirConta.tsx`
- `apps/cliente/src/screens/modals/ModalPermissaoPush.tsx`
- `apps/cliente/package.json` (adicionada dependência `@keepit/core-data: workspace:*`)

**Ambiente (não versionado normalmente, criado manualmente no sandbox):**
- `apps/cliente/node_modules/@keepit/core-data` (symlink manual para `packages/core-data`, necessário para `tsc` resolver o novo import sem rodar `pnpm install`)

## QA Results

### Review Date: 2026-07-28

### Reviewed By: Quinn (Test Architect & Quality Advisor)

### Escopo da revisão

Revisão independente da Story 0.4 contra `qa-gate.md` (7 checks), comparando as
telas de `apps/cliente/src/screens/{auth,perfil,modals}` com as 4 referências
visuais (`cliente-01-onboarding.png`, `cliente-09-criar-conta.png`,
`cliente-10-login.png`, `cliente-08-perfil.png`). Sem repo git e sem simulador —
validação visual empírica em device deferida (REQ-002, herdado dos gates 0.1/0.3).

### 7 Quality Checks

| # | Check | Resultado | Nota |
|---|-------|-----------|------|
| 1 | Code review | PASS | Kit de UI reutilizável, comentários [IDS]/[AUTO-DECISION], tipagem de rotas correta. |
| 2 | Unit tests | N/A | Sem testes de UI (decisão consciente 0.1/0.3, MVP casca). Port auth já testada na 0.2. |
| 3 | Acceptance criteria | PASS | AC1-AC4 atendidos a nível de código (ver abaixo). |
| 4 | No regressions | PASS | `pnpm --filter @keepit/cliente typecheck` → exit 0. File List restrito a apps/cliente/**. |
| 5 | Performance | N/A | Casca visual sem rede/persistência. |
| 6 | Security | PASS | Sem auth real, sem rede, sem segredos; signUp mock só recebe nome+telefone. |
| 7 | Documentation | PASS | Dev Agent Record exaustivo; docstrings citam FR/fonte. |

### Verificação de AC (independente)

- **AC1 — 9 telas/estados:** CONFIRMADO. 11 arquivos de tela substituem os stubs
  da 0.3 (Onboarding1/2/3, CriarConta, ConfirmacaoSMS, Login, EsqueciSenha,
  Perfil, Configuracoes, ExcluirConta, ModalPermissaoPush). Rotas de `AuthStack`/
  `PerfilStack` intactas — sem rename.
- **AC2 — fidelidade (tema claro #F6F7F3):** cores/tipografia 100% via
  `@keepit/ui-tokens`; grep confirmou zero hex hardcoded (única ocorrência é
  `#F6F7F3` em comentário de docstring). Fidelidade pixel deferida (REQ-002).
  **Ressalva:** o protótipo de onboarding é ESCURO, a impl é clara (ver REQ-004).
- **AC3 — navegação sem auth real:** CONFIRMADO. Login/CriarConta/ConfirmacaoSMS
  navegam à Home; `signUp`/`currentUser` chamados só para exercitar loading, sem
  bloquear navegação; validação client-side apenas.
- **AC4 — nenhuma regra hardcoded:** CONFIRMADO. Scan por `R$`/taxa/prazo/`%`
  retornou apenas percentuais de layout CSS e texto de comentário.

### Avaliação crítica das decisões do @dev

1. **Login social removido:** CORRETO — alinhado à decisão Rodada 2 (fora do MVP).
   Desvio documentado nas docstrings de CriarConta/Login. Não é issue.
2. **Gap email/senha (REQ-003, medium):** gap REAL de core-data (0.2). FR1/FR3
   coletam email+senha na UI, mas `Cliente` não modela esses campos e `auth.signUp`
   só aceita {nome, telefone}. Inócuo agora (sem submit real), mas **bloqueia auth
   real no Épico 1**. Escalado como follow-up ao stakeholder via Caio.
3. **Dep @keepit/core-data via symlink manual (ARCH-001, low):** aceitável para
   casca/typecheck; lockfile diverge do real. Precisa de `pnpm install` de
   reconciliação por @devops/CI antes de build/deploy.

### Gate Status

Gate: CONCERNS → docs/qa/gates/0.4-cliente-onboarding-auth-visual.yaml

Débitos não-bloqueantes rastreados: REQ-002 (validação em device), REQ-003 (gap
schema email/senha — bloqueia Épico 1), REQ-004 (tema do onboarding claro vs
protótipo escuro), ARCH-001 (pnpm install de reconciliação), DOC-001 (inferências
de layout sem captura dedicada).

### Recommended Status

Done (CONCERNS) — nenhum débito bloqueia o objetivo de casca visual. **Antes do
Épico 1**, o @po/stakeholder deve resolver REQ-003 (modelo de auth) e REQ-004
(tema do onboarding); @devops deve rodar o `pnpm install` de reconciliação (ARCH-001).
