# Story 12.2: Painel QA centralizado e identificação do build

## Status

Ready for Review

## Classificação

- Tipo: Frontend / Configuração
- Complexidade: Alta
- Escopo: somente builds Cliente autorizados para QA.

## Dependencies

- Story 12.1 — estado mock persistente e reset.

## Story

**Como** pessoa testadora,  
**quero** controles de cenário centralizados e identificação inequívoca do APK,  
**para** simular situações sem confundir estados artificiais com defeitos reais.

## Acceptance Criteria

1. Controles técnicos deixam de aparecer nas telas normais e ficam em um Painel QA centralizado.
2. O painel só é registrado quando a flag QA está ativa; produção não contém rota/acesso funcional ao painel.
3. Em QA, sete toques sobre a versão no Perfil abrem o painel, sem senha fixa embarcada.
4. O painel oferece reset, conta demo, relógio, inspeção/contingência de pedidos, pausa do avanço automático, simulações por domínio, callback de senha e prazo/restauração de exclusão.
5. Loading, vazio e erro podem ser simulados separadamente para pedidos, lojas, hubs, favoritos, perfil e busca.
6. Toda simulação ativa exibe “Estado simulado ativo”; reset limpa todas as simulações.
7. Perfil e painel exibem versão, build number/versionCode, commit SHA, datasource, ambiente e data do build.
8. Testes comprovam acesso em QA, ausência em produção e comportamento dos controles.

## Tasks / Subtasks

- [x] Centralizar configuração e metadados de build (AC: 2, 7).
- [x] Criar rota/tela do Painel QA e gesto de sete toques (AC: 1–4) — relógio/pausa, senha e exclusão foram compostos pelas Stories 12.7, 12.12 e 12.13.
- [x] Migrar/remover `DevStateToggle` e `OrderStatusDevAdvancer` das telas de produto (AC: 1, 4, 5) — favoritos foram integrados pela Story 12.11.
- [x] Implementar faixa global e limpeza de simulações (AC: 5, 6), incluindo favoritos e controles adicionados pelas stories consumidoras.
- [x] Testar matrizes QA/produção e metadados automatizáveis (AC: 2, 3, 7, 8) — APK QA gerado; observação em dispositivo permanece exclusivamente na Story 12.14.

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["feature-flag validation", "build-config review", "access tests"]

## Dev Notes

- Fonte: `docs/superpowers/specs/2026-09-04-estabilizacao-beta-android-cliente-design.md#6-painel-de-demonstraçãoqa` e `#20-identificação-e-rastreabilidade-do-build`.
- Componentes atuais: `apps/cliente/src/components/discovery/DevStateToggle.tsx` e `apps/cliente/src/components/pedidos/OrderStatusDevAdvancer.tsx`.
- Fonte de datasource: `apps/cliente/src/lib/dataSource.ts`; configuração EAS/app deve ser inspecionada, sem inventar nomes além dos existentes/aprovados.
- O painel é contingência; a Story 12.7 mantém avanço automático como padrão.

### Testing

- Testes de navegação/configuração para provar que produção não registra acesso.
- Smoke manual em APK QA e build equivalente de produção.

## 🤖 CodeRabbit Integration

- **Primary Type:** Frontend; **Secondary:** Deployment/Security; **Complexity:** High.
- **Primary Agents:** `@dev`, `@qa`; **Supporting:** `@architect`, `@github-devops`.
- [ ] Pre-Commit (`@dev`): flags, rotas e testes.
- [ ] Pre-PR (`@github-devops`): metadados e ausência do painel em produção.
- [ ] Pre-Deployment (`@github-devops`): configuração/segredos e rastreabilidade do artefato.
- **Self-Healing:** `@dev`, light, 2 iterações/15 min, CRITICAL auto-fix; HIGH documentado.
- **Focus Areas:** vazamento de ferramentas QA, valores hardcoded e divergência entre SHA declarado e bundle.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---:|---|---|
| 2026-09-04 | 0.1 | Draft inicial. | @sm (River) |
| 2026-09-05 | 0.2 | Gates locais registrados; build e smoke do APK bloqueados por indisponibilidade de rede do EAS. | @dev (Dex) |
| 2026-09-05 | 1.0 | Capacidades do painel completadas pelas stories consumidoras e APK QA mock identificado; smoke Android permanece no gate 12.14. | @po (Pax) / @qa (Quinn) |

## QA Results

### Atualização final — 2026-09-05

**Gate técnico: PASS / Ready for Review.** Rota protegida por flag, sete toques,
reset com feedback, faixa de simulação, metadados e controles de pedidos,
favoritos, recuperação de senha e exclusão foram compostos e cobertos pelos
gates SDD das Stories 12.2, 12.7, 12.11, 12.12 e 12.13. Produção continua sem
rota funcional do painel.

O APK QA mock existe: build EAS
`9ed5f830-46f3-44f6-a359-3daac0af86fb`, versão `1.0.0`, versionCode `7`,
datasource `mock`, ambiente `qa`, SHA de build `1cf88cf5f241db598cfa1841a42b7ce387aeb17f`.
O artefato **não foi instalado nem observado**; a conferência dos seis
metadados na UI e os controles no dispositivo permanecem no smoke da Story
12.14. Fonte: `docs/qa/epic-12-android-rc.md`.

### Registro histórico anterior ao APK e às stories consumidoras

### Resultado

`DONE_WITH_CONCERNS` em 2026-09-05. Os gates automatizados passaram, mas o APK QA ainda não havia sido gerado e o smoke manual não foi executado porque o host não conseguiu resolver `api.expo.dev`. A story permanecia `Draft` e nenhum critério dependente do artefato ou das capacidades futuras foi marcado como comprovado naquele snapshot.

### Fonte e configuração local inspecionadas

- Branch: `codex/story-12-1-mock-cliente`.
- HEAD anterior a este registro documental: `a49d3617f29eeddec9a80975c75ea7e1de013893`.
- Snapshot UTC da inspeção: `2026-09-05T01:05:25Z`.
- `apps/cliente/eas.json`: profile `demo-apk` com `distribution: internal`, `environment: preview`, `android.buildType: apk`, datasource `mock`, ambiente `qa` e flag QA `true`.
- `apps/cliente/app.json`: versão `1.0.0` e `versionCode` local `4`. Esse valor não é declarado como `versionCode` de artefato, pois `autoIncrement` depende do build remoto que não ocorreu.

### Gates automatizados

1. Focados de estado e persistência:

   ```bash
   pnpm --filter @keepit/core-data test -- src/mock/cliente-state.test.ts src/mock/cliente-state-store.test.ts
   ```

   Resultado: 2 arquivos e 37 testes passando.

2. Focados do Painel QA:

   ```bash
   pnpm --filter @keepit/cliente test -- src/config/buildInfo.test.ts src/lib/qaSimulation.test.ts src/lib/qaAccess.test.ts src/lib/qaOrderActions.test.ts src/lib/resetDemoScenario.test.ts
   ```

   Resultado: 5 arquivos e 34 testes passando.

3. Gates completos dos pacotes afetados:

   ```bash
   pnpm --filter @keepit/core-data typecheck && pnpm --filter @keepit/core-data test && pnpm --filter @keepit/cliente typecheck && pnpm --filter @keepit/cliente test
   ```

   Resultado: ambos os typechecks com exit code 0; `core-data` com 29 arquivos/571 testes e `cliente` com 27 arquivos/204 testes, sem falhas.

4. Prova focada da flag falsa:

   ```bash
   pnpm --filter @keepit/cliente test -- src/lib/qaAccess.test.ts src/config/buildInfo.test.ts
   ```

   Resultado: 2 arquivos e 10 testes passando, incluindo `getQaPerfilRouteNames(false) === []` e `resolveBuildInfo(..., { qaEnabled: 'false' }).qaEnabled === false`. A inspeção de `PerfilStack` confirma que `PainelQA` só é registrado a partir das rotas devolvidas pelo helper.

5. Ausência dos controles técnicos antigos:

   ```bash
   ! rg -n "DevStateToggle|OrderStatusDevAdvancer|useState<DevSimState>" apps/cliente/src
   ```

   Resultado: exit code 0 e nenhum match. A inspeção de `PainelQA` confirmou apenas os cinco domínios desta entrega: Pedidos, Lojas, Hubs, Perfil e Busca.

### APK QA e smoke

- CLI local: `eas-cli/23.2.0`.
- `eas whoami` falhou antes de autenticar ou consultar credenciais: `request to https://api.expo.dev/graphql failed, reason: getaddrinfo EAI_AGAIN api.expo.dev`.
- A tentativa read-only de resolver o profile com metadados reais também falhou pelo mesmo DNS:

  ```bash
  EXPO_PUBLIC_COMMIT_SHA="$(git rev-parse HEAD)" EXPO_PUBLIC_BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" eas config --platform android --profile demo-apk --json
  ```

- Nenhum `eas env:set` foi executado; logo, não há evidência de que SHA/data remotos estejam corretos e `eas env:list --environment preview` não pôde ser validado.
- Por segurança, `eas build --platform android --profile demo-apk` não foi iniciado depois do bloqueio. Build ID/URL, SHA do artefato, data do artefato e `versionCode` remoto: **não disponíveis**.
- Sem APK, os passos de login, inspeção dos seis metadados, sete toques, simulações, avanço manual, reset e ausência de controles técnicos no dispositivo ficaram **não executados**.
- A ausência da rota com flag falsa ficou coberta somente pelos testes automatizados e pela inspeção de `PerfilStack`, conforme o fallback previsto no plano.

### Cobertura e escopo adiado

- Comprovados localmente: validação do estado V2 e migração V1, persistência/reset das simulações, gate da rota QA, gesto de sete toques no helper, cinco domínios funcionais, avanço manual suportado, limpeza coordenada do cenário/carrinho e ausência dos controles inline antigos.
- Não comprovados em artefato: metadados sem fallback `local`/`unknown`, montagem/fluxos no APK e smoke manual completo.
- Naquele snapshot, favoritos, relógio/pausa, callback de senha e exclusão ainda pertenciam às stories consumidoras; todos foram entregues posteriormente e estão refletidos na atualização final acima.
