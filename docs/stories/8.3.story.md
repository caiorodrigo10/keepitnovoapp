# Story 8.3: Admin — lista de pedidos com filtros

## Status

Done

## MVP Pilot Classification

- Classificação: CORE
- Fonte: `docs/prd/07-plano-mvp-piloto.md`; `docs/orchestration/BLOCO-09-ADMIN-OPS-2026-08-13.md`
  ("8.3 | Lista de pedidos... com filtros... paginada | Leitura real.").
- Backend esperado: leitura administrativa real de `pedidos`, filtrável por
  status/hub/estabelecimento/cliente/intervalo de datas, sob RLS
  `is_admin()` (já coberta pela policy `ve_pedidos_relacionados`,
  `pode_ver_pedido` — Bloco 06, aplicada).
- Retomada futura: N/A — leitura direta, sem automação adiada.

## Data Mode

- Entidades: `pedidos` (leitura administrativa, todos os estabelecimentos).
- Modo padrão: mock — a tela `/pedidos` já existe e funciona 100% desde a
  Story 0.13/1.10, consumindo `client.admin.listAllOrders({ status })`
  sobre `db.pedidos`. Filtro por status já implementado na UI mock (15
  valores do enum). Nenhuma mudança de UI/mock esperada.
- Modo real: `DATA_SOURCE=supabase` habilita `AdminPort.listAllOrders`
  real, hoje `NotImplementedError(AdminPort, 'listAllOrders', 'Épico 8')`.
  Consulta `pedidos` (RLS `is_admin()` → vê todos), com filtro opcional de
  `status`.
- Compatibilidade mock: preservada.
- Modo híbrido: proibido nesta Story.

## Dependencies

- Story 3.7 (Done) — `admin_users`/`is_admin()` já existem.
- Bloco 06 (Done) — `pedidos`, RLS `ve_pedidos_relacionados`/
  `pode_ver_pedido` já aplicadas (`is_admin()` já cobre leitura irrestrita
  de admin).
- 🟠 **Decisão de escopo NÃO prescrita aqui:** o texto original do Épico
  (AC2) pede filtros por `hub`, `estabelecimento` e `cliente` (busca por
  nome/e-mail/telefone), além de `status` e intervalo de datas. A UI mock
  atual (Story 0.13) implementa **apenas** o filtro de `status`
  (`AdminPort.listAllOrders(filtros?: { status? })` — a assinatura da port
  não tem `hub_id`/`estabelecimento_id`/`busca`/intervalo de datas hoje).
  Estender a assinatura da port (`AdminPort.listAllOrders`) e a UI para os
  filtros adicionais é trabalho desta Story se @po confirmar como
  obrigatório — **não prescrito aqui em detalhe** (decisão de @architect/
  @dev sobre a forma exata da extensão de filtros, mesmo padrão de extensão
  de contrato já usado nas Stories 3.7/4.1). Se o piloto aceitar entregar
  primeiro só o filtro de `status` (já funcional) e os demais como
  iteração seguinte, isso deve ficar registrado como débito não-bloqueante,
  não como decisão silenciosa do @dev.
- ⚠️ **Paginação (texto original AC1 — "tabela paginada"):** a UI mock
  atual não pagina (lista tudo). Nº de pedidos esperado no piloto é baixo
  (poucos hubs/lojistas, `docs/PERGUNTAS_REGRAS_NEGOCIO.md` Rodada 1
  "Volume MVP") — decisão de @architect/@dev se a paginação real é
  necessária nesta Story ou se a lista sem paginação é aceitável para o
  volume do piloto (sinalizado, não prescrito).

## Complexidade

SIMPLE — a rota (`/pedidos`) e o contrato básico (`AdminPort.listAllOrders`
com filtro de `status`) já existem desde a Story 0.13/1.10 (mock funcional
completo). O trabalho real é: (a) adapter Supabase real de `listAllOrders`;
(b) decisão de escopo sobre os filtros adicionais (hub/estabelecimento/
cliente/datas) e paginação, delegada a @architect/@po.

## Scope

**Incluído:** adapter Supabase real de `AdminPort.listAllOrders` (filtro de
`status`, leitura de todos os pedidos sob RLS admin); colunas já existentes
na UI (número, cliente, loja, hub, status, valor, criado em, ações);
clique abrindo `/pedidos/[id]` (já existente, Story 8.4).

**Excluído (salvo decisão explícita de @po):** filtros adicionais
(hub/estabelecimento/cliente/intervalo de datas) além de `status`, se @po
não os confirmar como bloqueantes desta Story; paginação real, se o volume
do piloto não justificar.

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm qa", "typecheck do Admin", "testes unitários do adapter Supabase de listAllOrders", "revisão de @architect sobre a decisão de escopo de filtros adicionais e paginação"]
preferred_execution_model: "terra"
model_fallback: "luna"
architecture_review_model: "sol"

## Story

**As a** admin,
**I want** ver todos os pedidos com filtros úteis,
**so that** eu investigue casos e tome decisões.

## Acceptance Criteria

1. Rota já existente `/pedidos` (Admin web, entregue mock na Story 0.13)
   lista, com dados reais quando `DATA_SOURCE=supabase`, todos os pedidos.
   **Nota de fidelidade de interface:** o texto original do épico usa
   `/admin/pedidos` — esta Story preserva a rota já entregue (`/pedidos`),
   mesmo padrão já aplicado nas Stories 3.7/8.1. Tabela **paginada** —
   decisão de @architect/@dev sobre a necessidade real de paginação para o
   volume do piloto (ver Dependencies). [Fonte:
   `docs/prd/epics/8-admin-operacao.md` Story 8.3 AC1]

2. Filtros: status (já implementado em mock), hub, estabelecimento, cliente
   (busca por nome/e-mail/telefone), intervalo de datas. **Os 4 últimos NÃO
   existem hoje na UI/porta mock** — extensão de escopo sinalizada em
   Dependencies, decisão de @po sobre bloquear `Done` nesta Story. [Fonte:
   `docs/prd/epics/8-admin-operacao.md` Story 8.3 AC2]

3. Colunas: número, cliente, loja, hub, status, valor, criado em, ações
   (ver detalhe) — **já implementadas na tela mock**; nenhuma mudança de
   layout esperada, só a fonte de dados por trás muda quando
   `DATA_SOURCE=supabase`. [Fonte: `docs/prd/epics/8-admin-operacao.md`
   Story 8.3 AC3]

4. Clique abre detalhe do pedido (Story 8.4) — já roteado para
   `/pedidos/[id]`, existente desde a Story 0.13. [Fonte:
   `docs/prd/epics/8-admin-operacao.md` Story 8.3 AC4]

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** API/Adapter (leitura administrativa real de `pedidos`)
**Secondary Type(s):** Frontend (extensão opcional de filtros, decisão de
@po)
**Complexity:** Low — a tela e o filtro básico já existem; o trabalho real
é religar dados e decidir o escopo dos filtros adicionais.

### Specialized Agent Assignment

**Primary Agents:**

- @dev (Terra preferido; Luna como fallback) — adapter Supabase de
  `listAllOrders`.
- @architect (Sol) — decide o escopo de filtros adicionais e paginação.
- @qa

**Supporting Agents:**

- @ux-design-expert — não esperado, salvo se os filtros adicionais exigirem
  novos controles de UI fora do padrão já usado em `/clientes` (busca) e
  `/pedidos` (filtro de status).

### Quality Gate Tasks

- [ ] Architecture review (@architect, Sol): confirmar o escopo de filtros
  (status apenas vs. os 5 do texto original) e paginação ANTES do @dev
  implementar, evitando retrabalho.
- [ ] Pre-Commit (@dev): `pnpm qa`, typecheck do Admin, testes unitários
  antes de `In Review`.
- [ ] QA (@qa): validar filtro de status funcionando com dados reais, RLS
  admin vendo todos os pedidos, estados loading/vazio/erro preservados.
- [ ] Pre-PR (@devops): somente com autorização explícita do usuário.

### CodeRabbit Focus Areas

- RLS de `pedidos` (`is_admin()` já cobre leitura irrestrita) — nenhuma
  policy nova esperada, reforçar no code review.
- `service_role`/segredo Supabase não entram no bundle do Admin.
- Nenhum sucesso fictício: falha de rede/RLS exibe erro real.

### Self-Healing Configuration

**Expected Self-Healing:**

- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior:**

- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only (registrado para @qa)

## Tasks / Subtasks

- [x] **[@architect]** Decidir o escopo de filtros adicionais
  (hub/estabelecimento/cliente/datas) e a necessidade de paginação real
  para o volume do piloto. (AC: 1, 2)
- [x] **[@dev]** Implementar o adapter Supabase real de
  `AdminPort.listAllOrders` (filtro de `status`, e os demais filtros se
  confirmados pelo @architect), ordenado por `criado_em DESC` (espelha
  `idx_pedidos_estab`/`idx_pedidos_status`). (AC: 1, 2, 3)
- [x] **[@dev]** Se confirmados, estender `AdminPort.listAllOrders` e a UI
  de `/pedidos` com os filtros adicionais. (AC: 2)
- [x] **[@dev]** Atualizar/confirmar o adapter mock com paridade funcional
  para os filtros novos, se adicionados. (AC: 2; Data Mode)
- [x] Testes unitários do adapter Supabase (`listAllOrders` sem filtro,
  filtrado por status, filtros adicionais se implementados; RLS admin vendo
  todos). (AC: 1, 2, 3)
- [x] Rodar `pnpm qa`, `pnpm --filter @keepit/admin typecheck`; revisão
  manual contra `keepit-dev` (lista real de pedidos com filtro). (AC: 1-4)

## Dev Notes

### Contexto e limites

- A rota `/pedidos` (lista) e `/pedidos/[id]` (detalhe) já existem
  fielmente desde a Story 0.13 — o trabalho central desta Story é religar
  `listAllOrders` do mock para o adapter Supabase real. [Source:
  `apps/admin/app/(dashboard)/pedidos/page.tsx`]
- `usePedidosAdmin` (hook local do Admin) já consome
  `client.admin.listAllOrders({ status })` via `getAdminDataClient()` —
  nenhuma mudança de hook é esperada para o filtro de status; extensão para
  os filtros adicionais, se confirmada, muda a assinatura do hook e da
  port. [Source: `apps/admin/src/hooks/useAdminOps.ts`]
- `AdminPort.listAllOrders`/`forceCancelOrder` já têm mock funcional
  completo (`admin.mock.ts`) desde a Story 0.13/1.10 — o adapter Supabase
  (`admin.supabase.ts`) hoje lança `NotImplementedError(AdminPort,
  'listAllOrders', 'Épico 8')`. [Source:
  `packages/core-data/src/mock/admin.mock.ts`,
  `packages/core-data/src/supabase/admin.supabase.ts`]
- RLS de `pedidos` já cobre leitura administrativa irrestrita via
  `pode_ver_pedido` (`is_admin() OR cliente_id = auth.uid() OR
  estabelecimento_id = meu_estabelecimento_id()`), aplicada no Bloco 06.
  [Source: `apps/supabase/supabase/migrations/20260813004932_criar_pedidos.sql`]

### Testing

- Testes unitários do adapter Supabase: `listAllOrders` sem filtro
  retornando todos os pedidos (RLS admin), filtrado por `status`, ordenação
  `criado_em DESC`.
- `pnpm qa`.
- `pnpm --filter @keepit/admin typecheck`.
- Revisão manual: `/pedidos` lista pedidos reais, filtro de status
  funcionando, clique abre `/pedidos/[id]` com dados reais.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-13 | 0.1 | Draft CORE criado a partir do Épico 8 (Story 8.3) e do overlay do Bloco 09. Escopo de filtros adicionais (hub/estabelecimento/cliente/datas — presentes no texto do Épico mas ausentes da UI/porta mock atual) e paginação sinalizados como decisão de @architect/@po, não prescritos. Filtro por status já funcional em mock, só precisa religar dados reais. | @sm (River) |
| 2026-08-13 | 0.2 | Validado GO (8/10) — Status: Draft → Ready. @po resolveu o escopo: filtro de `status` + leitura real = CORE mandatório; filtros adicionais e paginação = débito não-bloqueante para o volume do piloto (ver Validação PO). | @po (Pax) |

| 2026-08-13 | 0.3 | Desenvolvimento iniciado (YOLO mode) — Status: Ready → InProgress | @dev (Dex) |
| 2026-08-13 | 0.4 | Implementação concluída (backend já aplicado via MCP; adapters/UI religados) — Status: InProgress → InReview | @dev (Dex) |
## Validação PO (Pax) — 2026-08-13

**Veredito: GO — 8/10 · Confiança: Alta**

Checklist 10 pontos: 1 Título PASS · 2 Descrição PASS · 3 ACs testáveis/com fonte PASS · 4 Escopo IN/OUT PASS · 5 Dependências PASS (Bloco 06 `pedidos`+RLS; 3.7) · 6 Complexidade PASS (SIMPLE) · 7 Valor PASS · 8 Riscos PASS · 9 Done PASS · 10 Alinhamento PRD/Épico PASS.

**[AUTO-DECISION] Filtros hub/estabelecimento/cliente/datas (AC2) e paginação (AC1) são obrigatórios para Done? → NÃO, débito não-bloqueante; filtro de `status` + leitura real são o CORE mandatório desta Story.** Razão: volume do piloto é baixo (Rodada 1: 4-5 hubs, poucos lojistas), então uma lista com filtro de status já atende a necessidade operacional de "investigar casos"; estender a assinatura da `AdminPort.listAllOrders` e a UI para os 4 filtros + paginação é iteração seguinte legítima, não requisito de operação. Registrado explicitamente como débito (não é decisão silenciosa do @dev — a Story já exigia validação do @po neste ponto). @architect pode antecipar os filtros se o custo for baixo, mas não bloqueia o Done.

**Delegação técnica aceitável:** forma exata da extensão de filtros e da paginação — @architect/@dev na implementação.

**Sem gaps bloqueantes.**

_Esta seção é preenchida pelo agente de desenvolvimento durante a
implementação._

### Agent Model Used

claude-sonnet-4-6 (Dex, @dev) — modo YOLO, Bloco 09 (9 stories implementadas em conjunto na mesma sessão, mesmos arquivos de adapter compartilhados entre Stories).

### Debug Log References

Nenhum log de debug dedicado — `pnpm qa` (lint+typecheck+test) executado na raiz do monorepo, todos os 27 pacotes/tasks verdes. Verificação manual contra `keepit-dev` NÃO executada nesta sessão (ambiente autônomo, sem navegador interativo) — fica como pendência de verificação manual do @qa, mesmo padrão de lacuna já registrado em Stories anteriores (2.5.1/2.6/3.2/3.5/3.7/4.1).

### Completion Notes List

- Implementado `AdminPort.listAllOrders` real: `SELECT` sobre `pedidos` (RLS `pode_ver_pedido`, `is_admin()` já cobre leitura irrestrita), filtro opcional de `status`, ordenado por `criado_em DESC`. [IDS] REUSE de `PEDIDO_COLUMNS`/`mapRowToPedido`/`fetchItensPorPedidoIds` (exportados de `order.supabase.ts`) — nenhuma duplicação de mapeamento.
- Filtros adicionais (hub/estabelecimento/cliente/datas) e paginação real permanecem como débito não-bloqueante, conforme decisão já registrada pelo @po na validação desta Story — volume do piloto não justifica.
- Gate: `pnpm --filter @keepit/core-data typecheck`, `pnpm --filter @keepit/admin typecheck`, `pnpm qa` (raiz) — todos verdes.

### File List

- `packages/shared-types/src/supabase.ts` (MODIFIED — reconciliação manual: colunas `clientes.bloqueado/motivo_bloqueio/bloqueado_em`, tabela NOVA `estabelecimentos_falhas`, RPCs `confirmar_lancamento_admin`/`forcar_cancelamento_pedido`/`bloquear_cliente`/`desbloquear_cliente`/`suspender_lojista`/`reativar_lojista`)
- `packages/core-data/src/ports/admin.port.ts` (MODIFIED — `refundQueue.process` estendido com `resultado`/`detalhe`; `payoutQueue` novo; `reactivateLojista`/`lojistaOrderCounts` novos; `FinancialDashboardResult` estendido)
- `packages/core-data/src/supabase/admin-errors.ts` (MODIFIED — união de RPC alargada; `NaoAutorizadoError`/`ResultadoInvalidoError`/`LancamentoNaoEncontradoError`/`TipoInvalidoError`/`PedidoNaoEncontradoError`/`ClienteNaoEncontradoError` novos)
- `packages/core-data/src/supabase/order-errors.ts` (MODIFIED — `ClienteBloqueadoError` novo)
- `packages/core-data/src/supabase/admin.supabase.ts` (MODIFIED — esqueleto ENCERRADO: `refundQueue`/`payoutQueue`/`listClientes`/`blockCliente`/`unblockCliente`/`listAllEstabelecimentos`/`suspendLojista`/`reactivateLojista`/`lojistaQualityView`/`lojistaOrderCounts`/`financialDashboard`/`listAllOrders`/`forceCancelOrder` implementados)
- `packages/core-data/src/supabase/order.supabase.ts` (MODIFIED — `PEDIDO_COLUMNS`/`mapRowToPedido`/`fetchItensPorPedidoIds`/`fetchPedidoPorId` exportados para reuso administrativo; mapeamento `CLIENTE_BLOQUEADO`)
- `packages/core-data/src/supabase/store.supabase.ts` (MODIFIED — `ESTABELECIMENTO_COLUMNS`/`mapRowToEstabelecimento`/`fetchHorarios` exportados para reuso administrativo)
- `packages/core-data/src/supabase/wallet.supabase.ts` (MODIFIED — `LANCAMENTO_COLUMNS`/`mapLancamentoToSaque`/`STATUS_MAP` exportados para reuso em `payoutQueue`)
- `packages/core-data/src/mock/admin.mock.ts` (MODIFIED — paridade mock dos itens acima)
- `packages/core-data/src/mock/order.mock.ts` (MODIFIED — `create` rejeita cliente bloqueado, paridade com o adapter real)
- `packages/core-data/src/supabase/admin.supabase.test.ts` (MODIFIED — 61 testes, incluindo os 37 novos do Bloco 09)
- `packages/core-data/src/supabase/order.supabase.test.ts` (MODIFIED — teste `CLIENTE_BLOQUEADO`)
- `packages/core-data/src/supabase/supabase-adapters.test.ts` (MODIFIED — esqueleto de `admin.supabase.ts` encerrado, teste genérico ajustado)
- `packages/core-data/src/mock/admin.mock.test.ts` (MODIFIED — novos testes de paridade)
- `packages/core-data/src/mock/order.mock.test.ts` (MODIFIED — teste de cliente bloqueado)
- `packages/core-data/src/index.test.ts` (MODIFIED — sentinela de stub trocado de `admin.refundQueue.list` para `store.getCatalog`, já que o esqueleto de `admin.supabase.ts` foi encerrado)
- `apps/admin/src/hooks/useAdminOps.ts` (MODIFIED — `usePayoutQueue`/`useLojistaOrderCounts` novos; `FinancialDashboardResult` vazio estendido)
- `apps/admin/src/lib/adminLabels.ts` (MODIFIED — `SAQUE_STATUS_LABEL` novo)
- `apps/admin/app/(dashboard)/reembolsos/[id]/page.tsx` (MODIFIED — `process` religado com `resultado`/`detalhe`; caminho "Marcar como erro" adicionado)
- `apps/admin/app/(dashboard)/lojistas/[id]/page.tsx` (MODIFIED — botão "Reativar lojista")
- `apps/admin/app/(dashboard)/qualidade-lojista/page.tsx` (MODIFIED — contagens entregues/cancelados/no-show + alerta visual >3/30d)
- `apps/admin/app/(dashboard)/financeiro/page.tsx` (MODIFIED — contagens + taxa de sucesso)
- `apps/admin/app/(dashboard)/saques/page.tsx` (NEW — fila de saques)
- `apps/admin/app/(dashboard)/saques/[id]/page.tsx` (NEW — marcar saque executado/erro)
- `apps/admin/app/(dashboard)/layout.tsx` (MODIFIED — link de navegação "Saques")

## QA Results

**Revisor:** Quinn (@qa, Guardian) · **Data:** 2026-08-13 · **Gate: PASS** · Confiança: Alta

### Evidência
- `pnpm qa` 27/27; core-data 475/475 (fresh); admin typecheck limpo.

### AC-by-AC
- **AC1 (rota `/pedidos` real) — PASS.** `listAllOrders` (`admin.supabase.ts:1390-1412`) lê `pedidos` via RLS admin (`pode_ver_pedido` = `is_admin() OR ...` já cobre), ordenado `criado_em DESC`, itens em lote (sem N+1). REUSE de `PEDIDO_COLUMNS`/`mapRowToPedido`/`fetchItensPorPedidoIds`. Paginação = débito pré-aprovado.
- **AC2 (filtros) — PARCIAL/PASS.** Filtro de status aplicado via `.eq('status', ...)` (teste `admin.supabase.test.ts:994`). Filtros hub/estab/cliente/datas = extensão de escopo **débito não-bloqueante** (decisão PO registrada; volume do piloto baixo).
- **AC3 (colunas) — PASS.** UI já entregue (Story 0.13); só a fonte de dados mudou.
- **AC4 (clique → detalhe) — PASS.** Roteamento `/pedidos/[id]` existente; detalhe é a Story 8.4.

### Débitos (pré-aprovados @po)
- Filtros hub/estab/cliente/datas + paginação real; verificação manual em browser.

**Decisão:** PASS → Status InReview → **Done**.
