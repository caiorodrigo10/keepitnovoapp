# Story 2.5.1: Bootstrap do cliente Supabase no app Cliente

## Status

Done

## MVP Pilot Classification

- Classificação: CORE
- Fonte: `docs/prd/07-plano-mvp-piloto.md`
- Backend esperado: um único cliente Supabase configurado no bootstrap Expo,
  com sessão persistente e injeção através de `packages/core-data`.
- Retomada futura: `docs/architecture/06-session-persistence.md` §§1–6.
- ClickUp: não sincronizado — conector indisponível nesta sessão; criar a tarefa
  filha do Épico 2 antes de iniciar implementação.

## Data Mode

- Entidades: clientes (sessão e autenticação).
- Modo padrão: mock.
- Modo real: `DATA_SOURCE=supabase` habilita o cliente Supabase persistente
  para a autenticação; não promove lojas, produtos ou pedidos a dados reais.
- Compatibilidade mock: com `DATA_SOURCE=mock`, o fluxo visual e as fixtures
  atuais continuam sendo a experiência padrão.
- Modo híbrido: não aplicável nesta Story; seleção por port exigiria uma Story
  arquitetural explícita.

## Dependencies

- Stories 2.3 e 2.3.1 concluídas.
- Esta Story deve estar `Done` antes da Story 2.6.
- Não depende de decisão de stakeholder; confirma a arquitetura já registrada.

## Complexidade

STANDARD — alteração aditiva em três fronteiras existentes, com risco de
regressão web × React Native mitigado pelos checks obrigatórios do Admin.

## Scope

**Incluído:** leitura `EXPO_PUBLIC_` com fallback compatível, opções aditivas
no client Supabase, compartilhamento de uma instância entre os oito adapters e
bootstrap explícito do Cliente com AsyncStorage.

**Excluído:** UI de login/SMS, `expo-secure-store`, bootstrap do Lojista,
mudança de RLS, `createServiceRoleClient()` e qualquer operação de deploy.

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm qa", "pnpm --filter @keepit/admin typecheck", "pnpm --filter @keepit/admin exec next build", "boundary review web × React Native"]
preferred_execution_model: "terra"
model_fallback: "luna"
architecture_review_model: "sol"

## Story

**As a** cliente autenticável,
**I want** que o app Cliente inicialize um único cliente Supabase com storage
persistente,
**so that** login, cadastro e re-login sobrevivam ao reinício do aplicativo sem
acoplar dependências React Native ao Admin web.

## Acceptance Criteria

1. Configuração pública usada no Expo é lida com prefixo `EXPO_PUBLIC_`; chaves
   `service_role` e Asaas não entram no bundle mobile.
2. `createClient()` aceita opções de storage sem importar AsyncStorage nem outra
   dependência React Native em `packages/supabase-client`.
3. `createDataClient()` recebe e reutiliza uma única instância de
   `SupabaseClient` nos adapters Supabase.
4. O bootstrap do Cliente injeta AsyncStorage antes do primeiro
   `getDataClient()`; Lojista e Admin não recebem dependência React Native.
5. Em device/simulador, uma sessão de Supabase Auth sobrevive a fechar e abrir
   novamente o app Cliente.
6. `pnpm --filter @keepit/admin typecheck` e
   `pnpm --filter @keepit/admin build` continuam verdes.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Integration / Security / Infrastructure
**Secondary Type(s):** Backend adapter e bootstrap React Native
**Complexity:** STANDARD

### Specialized Agent Assignment

**Primary Agents:**

- @dev (Terra preferido; Luna como fallback)
- @qa

**Supporting Agents:**

- @architect (Sol para revisão do boundary web × React Native)
- @devops (commit e integração local)

### Quality Gate Tasks

**Quality Gate Tasks:**

- [ ] Pre-Commit (@dev): `pnpm qa` e testes direcionados antes de `In Review`.
- [ ] Architecture review (@architect): confirmar que o pacote compartilhado não
  importa AsyncStorage e que o Admin não ganha dependência React Native.
- [ ] QA (@qa): validar ACs, sessão em device/simulador e regressão do Admin.
- [ ] Pre-PR (@devops): somente se o usuário autorizar operação remota.

### CodeRabbit Focus Areas

**CodeRabbit Focus:**

- Segredos e uso exclusivo de variáveis públicas no bundle Expo.
- Fronteira `app → core-data → supabase-client` e ausência de client duplicado.
- Persistência, unsubscribe e falhas de bootstrap sem tela branca.

### Self-Healing Configuration

**Expected Self-Healing:**

- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL
- CRITICAL: corrigir e reexecutar a verificação antes de encaminhar ao gate.
- HIGH: documentar para o gate de QA; MEDIUM e LOW: não bloqueiam o fluxo do
  executor.
- QA aplica a configuração `full` do `core-config.yaml` (3 iterações, 30 min,
  CRITICAL/HIGH) no início da revisão de QA.

## Tasks / Subtasks

- [x] Atualizar `readEnv` e `.env.example` para aceitar primeiro
  `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_ANON_KEY` no Expo e
  preservar o fallback sem prefixo para servidor/scripts. (AC: 1)
- [x] Adaptar `createClient()` para receber opções de criação sem importar
  storage React Native no package compartilhado. (AC: 2)
- [x] Propagar o cliente único por `CreateDataClientOptions` para os oito
  adapters Supabase existentes. (AC: 3)
- [x] Configurar o bootstrap do app Cliente com AsyncStorage antes da criação do
  data client. (AC: 4)
- [x] Adicionar/atualizar testes dos adapters e executar typechecks do Cliente e
  Admin. (AC: 2, 3, 6)
- [ ] Validar manualmente signup/login, fechar o app e reabrir com sessão ativa.
  (AC: 5) — **NÃO executada nesta sessão.** Exige device/simulador físico com
  `EXPO_PUBLIC_SUPABASE_URL`/`EXPO_PUBLIC_SUPABASE_ANON_KEY` reais, indisponível
  neste ambiente de desenvolvimento autônomo. Registrada como verificação
  manual pendente para @qa/usuário — ver Dev Agent Record.

## Dev Notes

### Contexto e limites

- Esta Story precede 2.6. A tela de login não deve absorver infraestrutura de
  storage ou plataforma. [Source: `docs/architecture/06-session-persistence.md` §6]
- A fronteira correta é `app → core-data → supabase-client`. A única exceção
  deliberada é o módulo de bootstrap do Cliente, que constrói o client com
  AsyncStorage e o injeta em `getDataClient()`; telas e hooks continuam
  importando somente `@keepit/core-data`. [Source:
  `docs/architecture/06-session-persistence.md` §§1.2 e 3.3]
- AsyncStorage é a decisão vigente somente para o app Cliente neste MVP;
  SecureStore e o app Lojista ficam fora desta Story. [Source:
  `docs/architecture/06-session-persistence.md` §4 e §6]
- Não reativar SMS, confirmação de telefone, login social, confirmação de
  e-mail ou qualquer UI nova. [Source:
  `docs/prd/epics/2-auth-cliente.md` e
  `docs/architecture/06-session-persistence.md` §5–§7]

### Segurança

- Chaves Asaas e `service_role` ficam exclusivamente no servidor/Edge Function;
  nenhuma pode entrar no bundle mobile. [Source:
  `docs/architecture/05-security.md` §4.1]
- RLS e ownership continuam obrigatórios; esta Story não altera policies sem
  revisão de dados/segurança. [Source: `docs/architecture/05-security.md` §3]

### Arquivos candidatos

- `packages/supabase-client/src/index.ts`
- `packages/core-data/src/supabase/*`
- `apps/cliente/src/**` (bootstrap/navegação)
- `.env.example`
- testes adjacentes aos adapters e bootstrap

Os caminhos finais precisam ser confirmados pelo @architect no plano de
implementação antes do @dev modificar código.

### Sequência obrigatória

1. Fazer o bootstrap síncrono por import de módulo no topo de `App.tsx` (ou
   módulo equivalente) antes de qualquer hook/navegador que chame
   `getDataClient()`; `getDataClient()` memoiza a primeira configuração.
2. Para `source: 'supabase'`, criar ou receber uma única instância e passá-la
   aos oito factories; não alterar as assinaturas opcionais já existentes dos
   adapters.
3. Não alterar `createServiceRoleClient()`: ele mantém
   `persistSession: false` e não pode entrar em app cliente.

### Testing

- Manter testes junto aos packages afetados (`packages/*/src/**/*.test.ts`).
- Verificar o cliente injetado nos oito adapters e o fallback de ambiente.
- Executar `pnpm --filter @keepit/cliente typecheck`,
  `pnpm --filter @keepit/admin typecheck` e
  `pnpm --filter @keepit/admin exec next build`, além de `pnpm qa`. O script
  `build` atual do pacote Admin é apenas um placeholder e não satisfaz esta
  evidência.

## Testing

- `pnpm qa`.
- Testes unitários de `supabase-client` e `core-data` afetados.
- `pnpm --filter @keepit/cliente typecheck`.
- `pnpm --filter @keepit/admin typecheck`.
- `pnpm --filter @keepit/admin exec next build`.
- Device/simulador: autenticar, encerrar o processo, reabrir e confirmar sessão.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-07-31 | 0.1 | Draft criado como pré-requisito técnico de 2.6 a partir da arquitetura de persistência de sessão. | @sm (River) |
| 2026-07-31 | 0.2 | Validated GO (9/10) — Status: Draft → Ready; corrigidos executor/gate, fronteira de bootstrap, auto-healing e completude do template. | @po (Pax) |
| 2026-08-12 | 0.3 | Development started (yolo mode) — Status: Ready → InProgress. | @dev |
| 2026-08-12 | 0.4 | Development complete — Status: InProgress → InReview. AC1-AC4 e AC6 implementadas e verificadas por comando; AC5 registrada como verificação manual pendente (exige device/simulador). | @dev |
| 2026-08-12 | 0.5 | QA Gate CONCERNS — Status: InReview → Done. AC1-AC4/AC6 verificadas independentemente (8/8 + 109/109 + 30/30 testes, typechecks, next build, Admin RN-free); AC5 pendente de device (REL-001, medium); worktree divergente registrado como débito de integração (MNT-001, low). Sem issues critical/high. | @qa |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Dex/@dev), modo YOLO autônomo.

### Debug Log References

- `pnpm --filter @keepit/cliente typecheck` → PASS (sem output, exit 0).
- `pnpm --filter @keepit/admin typecheck` → PASS (sem output, exit 0).
- `pnpm --filter @keepit/admin exec next build` → PASS. Build Next.js/Turbopack completo, 13 páginas geradas (`/aprovacoes` incluída, sem erro de `Suspense`/`useSearchParams` — issue externa pré-existente já não reproduz no estado atual do working tree).
- `pnpm qa` → PASS. 27/27 tasks do Turbo (lint/typecheck/test em todos os workspaces). Destaques: `@keepit/supabase-client:test` 8/8, `@keepit/core-data:test` 109/109, `@keepit/cliente:test` 30/30 (inclui os 3 arquivos de teste novos/atualizados desta story).
- `pnpm --filter @keepit/admin why react-native` e `... why @react-native-async-storage/async-storage` → nenhuma ocorrência (grafo de dependências do Admin livre de RN, confirmação empírica do critério de saída não-negociável de `docs/architecture/06-session-persistence.md` §6).
- CodeRabbit self-healing (light, CRITICAL only): **não executado** — `~/.local/bin/coderabbit auth status` retornou `signed out` neste ambiente; sem credenciais para autenticar de forma autônoma. Seguido o path de graceful degradation do `core-config.yaml` (`skip_if_not_installed`) por analogia — revisão automatizada fica pendente para @qa/@devops, que têm acesso ao WSL configurado.

### Completion Notes List

- **AC1** — `readEnv()` em `packages/supabase-client/src/index.ts` agora lê `EXPO_PUBLIC_{name}` primeiro, com fallback para `{name}` sem prefixo. `.env.example` ganhou `EXPO_PUBLIC_SUPABASE_URL`/`EXPO_PUBLIC_SUPABASE_ANON_KEY` (duplicatas documentadas da URL/anon key já existentes) e `EXPO_PUBLIC_DATA_SOURCE` (novo — ver nota abaixo). `SUPABASE_SERVICE_ROLE_KEY` e `ASAAS_*` não ganharam prefixo.
- **AC2** — `CreateClientOptions` (`storage?`, `persistSession?`, `autoRefreshToken?`) é aditivo e 100% opcional; `storage` usa o tipo `SupportedStorage` re-exportado por `@supabase/supabase-js` (via `@supabase/auth-js`), sem nenhum import de módulo React Native no pacote. `createServiceRoleClient()` não foi tocado.
- **AC3** — `CreateDataClientOptions.supabaseClient?` em `packages/core-data/src/index.ts`; quando presente, a MESMA instância é passada às 8 factories (`createAuthSupabase(client)`, etc.) — os 8 adapters já aceitavam `client?` desde a Story 1.9, nenhuma assinatura mudou.
- **AC4** — `apps/cliente/src/lib/dataClientBootstrap.ts` (novo, `[IDS] CREATE` — justificado no próprio arquivo: nenhum bootstrap de `DataClient` existia em nenhum app) é importado como o PRIMEIRO import de `App.tsx`, antes de `RootNavigator`. Decisão de design (não especificada literalmente na story, registrada aqui como `[AUTO-DECISION]`): o bootstrap só monta o client Supabase quando `EXPO_PUBLIC_DATA_SOURCE === 'supabase'` — ausente/qualquer outro valor mantém o app 100% mock. Reason: `process.env.DATA_SOURCE` (sem prefixo) não é inlineado pelo Expo em runtime RN (confirmado em `docs/architecture/06-session-persistence.md` §1.4a), então chamar `getDataClient({ source: 'supabase', ... })` incondicionalmente forçaria toda instalação local a ter `EXPO_PUBLIC_SUPABASE_URL/ANON_KEY` configuradas só para abrir o app — quebraria a preservação obrigatória de `DATA_SOURCE=mock` como default seguro. Se `createClient()` lançar (env ausente), o bootstrap captura o erro, faz `console.warn` e cai para `getDataClient({ source: 'mock' })` — sem tela branca no boot (CodeRabbit Focus Area da story), mesmo espírito do guard AC7 de `RootNavigator.tsx` (Story 2.3.1). `apps/lojista` e `apps/admin` não foram tocados — zero dependência React Native nova no grafo do Admin (confirmado por `pnpm why`).
- **AC5** — **não implementável em CI/dev container.** Task de verificação manual (signup → matar processo → reabrir → sessão ativa) deixada como `[ ]` no story file, com nota explícita. Não foi simulada nem marcada como concluída artificialmente.
- **AC6** — `pnpm --filter @keepit/admin typecheck` e `pnpm --filter @keepit/admin exec next build` verdes (ver Debug Log). Nota: a story menciona "o script `build` atual do pacote Admin é apenas um placeholder" — hoje `apps/admin/package.json` já tem `next build` real via Turbopack (não é mais placeholder); rodei via `exec next build` conforme instruído, que é o comando literal executado.
- **[IDS] Decisões de reuso** — `packages/core-data`: REUSE das 8 factories/assinaturas `client?` já existentes (Story 1.9), zero mudança de assinatura. `packages/supabase-client`: ADAPT de `createClient()`/`readEnv()` (mudança aditiva, <30% do arquivo, não quebra `createServiceRoleClient()` nem nenhum dos 8 chamadores). `apps/cliente/src/lib/dataClientBootstrap.ts`: CREATE justificado inline (ver acima). Testes: ADAPT de `packages/core-data/src/index.test.ts` (novo describe block) e CREATE de `packages/supabase-client/src/index.test.ts` + `apps/cliente/src/lib/dataClientBootstrap.test.ts` (pacotes sem teste de `createClient()`/bootstrap antes desta story).
- **Achado durante a implementação, fora do escopo desta story:** ao rodar `.worktrees/story-2.5.1` (worktree dedicado, branch `story/2.5.1-bootstrap-client`), encontrei um commit prévio (`25da602`, "feat(cliente): bootstrap Supabase session client", datado 2026-07-31) implementando esta MESMA story de forma independente, com gate de QA já registrado em `docs/qa/gates/2.5.1-bootstrap-cliente-supabase.yaml` (verdict `CONCERNS`, AC5 `BLOCKED` pelo mesmo motivo — sem device/simulador). Esse commit nunca foi mesclado a `main`; a story em `main` foi resetada para `Ready` com Dev Agent Record vazio pelos commits `baa0d0c`/`0e1d350`/`702f4c5` (2026-08-12), o que sugere que a tentativa de 07-31 foi descartada deliberadamente em favor de um novo ciclo. **Não mesclei, apaguei nem toquei esse worktree** — fora do meu escopo (evitar operação destrutiva de git sem autorização). Meu diff nesta sessão foi feito inteiramente em `main` (working tree principal), com nomes de arquivo diferentes (`dataClientBootstrap.ts` em vez de `dataClient.ts`) e semântica parecida, mas não idêntica (uso de `EXPO_PUBLIC_DATA_SOURCE`). **Risco para @qa/orquestrador:** os dois branches (`main` local e `story/2.5.1-bootstrap-client`) agora contêm implementações divergentes da mesma story; recomendo reconciliar ou descartar explicitamente o worktree antigo antes do próximo passo de push/PR.

### File List

**Modificados:**
- `packages/supabase-client/src/index.ts` — `readEnv()` com prefixo `EXPO_PUBLIC_`; `CreateClientOptions` + `createClient(options)`.
- `packages/supabase-client/package.json` — script `test` de `echo skipped` para `vitest run`; `vitest` adicionado a `devDependencies`.
- `packages/core-data/src/index.ts` — `CreateDataClientOptions.supabaseClient?`; propagação da instância única aos 8 adapters.
- `packages/core-data/src/index.test.ts` — novo describe `createDataClient — supabaseClient injetado (Story 2.5.1, AC3)`.
- `apps/cliente/App.tsx` — import de `./src/lib/dataClientBootstrap` como primeira linha do arquivo.
- `apps/cliente/package.json` — dependência `@keepit/supabase-client` adicionada.
- `.env.example` — `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY`, `EXPO_PUBLIC_DATA_SOURCE` documentadas.
- `pnpm-lock.yaml` — atualizado por `pnpm install` (nova dependência `@keepit/supabase-client` em `apps/cliente`, `vitest` em `packages/supabase-client`).
- `docs/stories/2.5.1.story.md` — este arquivo (Status, Tasks, Change Log, Dev Agent Record).

**Criados:**
- `apps/cliente/src/lib/dataClientBootstrap.ts` — bootstrap do `DataClient` do Cliente (AC4).
- `apps/cliente/src/lib/dataClientBootstrap.test.ts` — testes do bootstrap (modo mock default, modo supabase, degradação em falha).
- `packages/supabase-client/src/index.test.ts` — testes de `createClient()`/`createServiceRoleClient()` (AC1, AC2).
- `packages/supabase-client/vitest.config.ts` — config vitest do pacote (não existia; script `test` era placeholder).

## QA Results

### Review Date: 2026-08-12

### Reviewed By: Quinn (Test Architect) — revisão independente (executor foi @dev)

### Veredito: CONCERNS

Gate limpo de issues critical/high. CONCERNS motivado exclusivamente por AC5
(persistência em device, não verificável neste ambiente) e pelo débito de
reconciliação do worktree divergente — ambos registrados abaixo. Todos os ACs
funcionais verificáveis foram atendidos e confirmados por reexecução própria,
não por confiança no relato do @dev.

### 7 Checks de Qualidade

1. **Code review — PASS.** Fronteira `app → core-data → supabase-client`
   preservada; `dataClientBootstrap.ts` é a única exceção deliberada e é o único
   ponto do app Cliente que importa `@keepit/supabase-client`/`AsyncStorage`.
   `CreateClientOptions` é 100% aditivo/opcional; `createServiceRoleClient()`
   intocado; nenhum client Supabase duplicado (instância única propagada aos 8
   factories). As menções a `react-native`/`async-storage` em
   `supabase-client/src` são apenas JSDoc/comentário — nenhum import real.
2. **Testes unitários — PASS.** Reexecutados: `@keepit/supabase-client` 8/8,
   `@keepit/core-data` 109/109 (inclui o teste AC3 que prova, via
   `onAuthStateChange`, que a instância injetada é a de fato usada — e não uma
   por adapter), `@keepit/cliente` 30/30 (inclui 4 testes do bootstrap: mock
   default, valor ≠ `supabase`, injeção `supabase`, degradação falha→mock).
   Cobertura adequada para o escopo aditivo.
3. **Acceptance Criteria** — AC1 MET, AC2 MET, AC3 MET, AC4 MET, AC6 MET;
   **AC5 PENDING_MANUAL** (persistência de sessão em device/simulador —
   inverificável sem device + credenciais reais; condição explícita de CONCERNS,
   NÃO reprovada por ausência de device).
4. **Não-regressão — PASS.** `admin typecheck` e `admin exec next build` verdes
   (17 rotas, sem erro de `Suspense`/`useSearchParams`); mock permanece o default
   seguro e navegável (bootstrap só ativa Supabase com
   `EXPO_PUBLIC_DATA_SOURCE=supabase`, coberto por teste).
5. **Performance — PASS.** Bootstrap é síncrono no module-eval; não bloqueia a
   inicialização e, em falha de `createClient()`, degrada para mock com
   `console.warn` — sem tela branca no boot.
6. **Segurança (CRÍTICO) — PASS.** `SUPABASE_SERVICE_ROLE_KEY` e `ASAAS_*` sem
   prefixo `EXPO_PUBLIC_` no `.env.example` (fora do bundle mobile);
   `pnpm --filter @keepit/admin why react-native` e `... async-storage`
   retornam vazio (Admin comprovadamente RN-free); `apps/lojista` não importa
   `@keepit/supabase-client`; story sem migration — RLS/schema não alterados;
   `createServiceRoleClient()` mantém `persistSession:false` e intocado.
7. **Documentação — PASS.** Story coerente com o código; Dev Agent Record
   completo e transparente quanto à divergência de worktree.

### Issues (nenhum critical/high)

| ID | Severidade | Categoria | Achado | Recomendação |
|----|-----------|-----------|--------|--------------|
| REL-001 | medium | reliability | AC5 (sessão sobrevive a fechar/reabrir o app) não validada — ambiente sem device/simulador nem credenciais reais. | Antes do piloto com `DATA_SOURCE=supabase`: signup/login em device → matar processo → reabrir → confirmar sessão ativa. |
| MNT-001 | low | maintainability | Worktree `.worktrees/story-2.5.1` (branch `story/2.5.1-bootstrap-client`, commit `25da602`) tem implementação divergente e nunca mesclada da MESMA story, com gate próprio CONCERNS. | Débito de integração para @devops: reconciliar ou descartar explicitamente o worktree antigo antes de push/PR. Não bloqueia o código em `main`. |

### Gate Status

Gate: CONCERNS → docs/qa/gates/2.5.1-bootstrap-cliente-supabase.yaml
