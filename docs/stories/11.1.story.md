# Story 11.1: Fundação visual mobile-inspired do Painel Admin

## Status

Done

## MVP Pilot Classification

- Classificação: UI_ONLY
- Fonte: `docs/prd/epics/11-experiencia-painel-admin.md`
- Backend esperado: N/A — apresentação local ao `apps/admin`.
- Retomada futura: N/A

## Dependencies

- Épico 10: painel, sessão mock e fixtures navegáveis.
- Tokens: `packages/ui-tokens/src/tokens.json`.

## Story

**Como** pessoa operadora do Keepit,
**quero** um painel com hierarquia, áreas de toque e superfícies consistentes
com os apps mobile,
**para** analisar e operar dados com menos esforço visual.

## Acceptance Criteria

1. `globals.css` garante fundo dark, Hanken Grotesk, renderização tipográfica
   suave e foco visível usando `accent-brand`.
2. `Button` e `Card` usam apenas tokens existentes, com altura mínima de 44 px
   para CTA, raios `md`/`card`, bordas e sombras coerentes.
3. O layout do dashboard tem navegação mais espaçada, área semântica nomeada,
   conteúdo centralizado em largura legível e padding responsivo.
4. O formulário de login segue o mesmo padrão de campo/CTA sem mudar a lógica
   de sessão.
5. Login, logout, links da sidebar e carregamento das listas continuam
   funcionais; `pnpm --filter @keepit/admin typecheck` passa.

## Tasks / Subtasks

- [x] Refinar base global, layout de autenticação e dashboard.
- [x] Atualizar `Button`, `Card` e campos de login com tokens compartilhados.
- [x] Validar fluxo visual e teclado no Admin em execução.

## Executor Assignment

executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: ["pnpm --filter @keepit/admin typecheck", "manual keyboard and responsive check"]

## Dev Notes

- Não introduzir biblioteca de componentes nem valores de cor/raio fora de
  `@keepit/ui-tokens`.
- Esta é uma melhoria de fundação deliberadamente limitada; aplicações nos
  campos e CTAs remanescentes pertencem à Story 11.2.

## 🤖 CodeRabbit Integration

- Tipo: Frontend; complexidade: baixa.
- Pre-commit (`@dev`): revisar acessibilidade, responsividade e consistência de
  tokens.
- Pre-PR (`@github-devops`): comparar o Admin com o layout atual e garantir que
  rotas/sessão não foram afetadas.

## Validação (subtask 3 — @ux-design-expert)

Validado em `feat/epic11-admin-visual` (worktree `block-15-admin-visual`),
Next.js 16.2.12 rodando em `http://localhost:3111` via `pnpm --filter
@keepit/admin dev`.

Conformidade com os Acceptance Criteria:

- **AC1** — `app/globals.css`: fundo dark (`#0c0e08`), `font-family:
  var(--font-hanken)`, `-webkit-font-smoothing: antialiased`,
  `text-rendering: optimizeLegibility` e `:focus-visible { outline: 2px solid
  #75dc8d }`. O valor `#75DC8D` corresponde exatamente ao token
  `colors.dark.accent.brand` em `@keepit/ui-tokens`. ✅
- **AC2** — `Button` usa `min-h-11` (44 px), `rounded-md` e apenas tokens
  (`bg-accent-brand`, `text-bg-shell`, `bg-bg-elevated`, `accent-warning`).
  `Card` usa `rounded-card`, `shadow-card`, `border-border-subtle`,
  `bg-bg-surface`. Todos os tokens referenciados existem em `tokens.json`. ✅
- **AC3** — `(dashboard)/layout.tsx`: `<nav aria-label="Navegação principal">`
  com espaçamento ampliado (`gap-1.5`, links `py-2.5`), `<main>` semântico,
  conteúdo centralizado (`mx-auto w-full max-w-6xl`) e padding responsivo
  (`p-6 sm:p-8 lg:p-10`). ✅
- **AC4** — `(auth)/login/page.tsx`: campos e CTA no mesmo padrão
  (`min-h-12`, `focus:border-accent-brand`), lógica de sessão intacta
  (`useAdminSession().signIn`, redirecionos preservados). ✅
- **AC5** — `pnpm --filter @keepit/admin typecheck` passa sem erros. Rotas
  servidas: `/` → 307 (redirect para login), `/login` → 200, `/aprovacoes` →
  200, `/hubs` → 200. Nenhum erro/warning de hidratação no log do dev server.
  Logout continua ligado (`AdminSidebarFooter` → `signOut()`). ✅

Validação de teclado e responsividade:

- **Foco visível** — sem overrides de `tabIndex` no Admin (ordem de tab segue
  o DOM natural: e-mail → senha → CTA). Inputs suprimem o outline padrão
  (`focus:outline-none`) mas expõem foco por mudança de borda
  (`focus:border-accent-brand`); botões e links da sidebar recebem o outline
  global `:focus-visible` (2 px `accent-brand`). Foco keyboard visível em toda
  a superfície.
- **Responsividade** — nenhum uso de `w-screen`/`overflow-x` forçado; conteúdo
  restrito por `max-w-md` (login) e `max-w-6xl mx-auto` (dashboard) com padding
  responsivo, sem overflow horizontal na área de conteúdo.
- **Fluxos** — login (`signIn`), logout (`AdminSidebarFooter`), links da
  sidebar (`next/link`, todas as 9 seções) e carregamento das listas
  permanecem funcionais.

Observação: campos/CTAs remanescentes das telas de detalhe do dashboard já
usam `focus:border-accent-brand` consistente, mas o refino completo deles é
escopo da Story 11.2 (conforme Dev Notes).

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-08-13 | 0.1 | Subtasks 1–2 (base global, layout auth/dashboard, `Button`/`Card`/campos com tokens) implementadas. | @ux-design-expert (Uma) |
| 2026-08-13 | 0.2 | Subtask 3: validação de fluxo visual, teclado e responsividade no Admin em execução; `typecheck` OK; AC1–AC5 confirmados. Status `In Progress` → `InReview`. | @ux-design-expert (Uma) |
| 2026-08-13 | 0.3 | Quality gate independente: `git diff feat/modo-demo-mock..HEAD -- apps/admin` sem diferenças (código de AC1-4 já presente na base, commit `3af1c37`); código relido em `globals.css`, `Button.tsx`, `Card.tsx`, `(dashboard)/layout.tsx`, `(auth)/login/page.tsx` e confirmado contra AC1-4; valores conferidos em `packages/ui-tokens/src/tokens.json` (`accent.brand` = `#75DC8D`, `radii.card` = 16, `shadows.card`) — nenhum token inventado. `pnpm --filter @keepit/admin typecheck` reexecutado, OK. Servidor subido localmente (`next dev -p 3112`): `/` → 307, `/login` → 200, `/aprovacoes` → 200, `/hubs` → 200, sem erros/warnings de hidratação no log. Lógica de sessão (`signIn`/`signOut`) e sidebar intactas. Status `InReview` → `Done`. | @dev (Dex) |

## QA Results

**Gate:** PASS (quality gate `@dev`, per Executor Assignment do Épico 11 — não é o QA formal `@qa`)

- AC1 — confirmado em `apps/admin/app/globals.css` (fundo `#0c0e08`, `font-family: var(--font-hanken)`, antialiasing, `:focus-visible { outline: 2px solid #75dc8d }`, valor igual a `colors.dark.accent.brand`).
- AC2 — confirmado em `Button.tsx` (`min-h-11`, `rounded-md`, apenas classes de tokens) e `Card.tsx` (`rounded-card`, `shadow-card`, `border-border-subtle`, `bg-bg-surface`); todos os tokens existem em `tokens.json`.
- AC3 — confirmado em `(dashboard)/layout.tsx`: `nav aria-label="Navegação principal"` com `gap-1.5`/`py-2.5`, `main` semântico, `mx-auto w-full max-w-6xl`, padding responsivo `p-6 sm:p-8 lg:p-10`.
- AC4 — confirmado em `(auth)/login/page.tsx`: campos `min-h-12` com `focus:border-accent-brand`, CTA usando `Button`, lógica de sessão (`useAdminSession().signIn`, redirect em `useEffect`) inalterada.
- AC5 — `pnpm --filter @keepit/admin typecheck` OK; rotas testadas via `curl` (`/` 307, `/login`/`aprovacoes`/`hubs` 200), sem erros no log do dev server; `signOut` (`AdminSidebarFooter`) e links da sidebar (`next/link`, 9 seções) intactos.
- Observação (não bloqueante): `git diff` contra `feat/modo-demo-mock` não mostra alterações em `apps/admin` porque o código que satisfaz AC1-4 já estava presente no branch-point (commit `3af1c37`, backend blocos 01-03); esta story documentou/validou fundação já implementada, não introduziu regressão nem escopo fora do combinado.
- Nenhum item FAIL encontrado. Escopo remanescente (campos/CTAs de telas de detalhe) permanece corretamente delegado à Story 11.2, conforme Dev Notes.
