# Story 5.7: Busca — resultados por loja

## Status

Done

## MVP Pilot Classification

- Classificação: SIMPLE
- Fonte: `docs/prd/07-plano-mvp-piloto.md` (Épico 5, linha "5.6–5.7 |
  SIMPLE | Busca case-insensitive do PostgreSQL; sem ranking
  especializado.")
- Backend esperado: reaproveita `StorePort.listByHub` (real pela Story
  5.2) com filtro de texto case-insensitive client-side (equivalente a
  ILIKE) — sem Edge Function/RPC dedicada, sem ranking.
- Retomada futura: nenhuma parte é `LATER`.

## Data Mode

- Entidades: `estabelecimentos` (via `estabelecimentos_hubs`).
- Modo padrão: mock (`DATA_SOURCE=mock`) — `useSearchLojas` já
  implementa a busca client-side sobre `store.mock.ts#listByHub`, sem
  alteração de comportamento por esta Story além do escopo de hub
  (herdado da Story 5.6, ver Dependencies).
- Modo real: `DATA_SOURCE=supabase` — a mesma composição passa a operar
  sobre dados reais assim que a Story 5.2 (`listByHub`) estiver ativa.
- Compatibilidade mock: preservada integralmente.
- Modo híbrido: proibido nesta Story.

## Dependencies

- **Decisão de escopo de hub (`DEFAULT_HUB_ID` → `cart.hubId` + guard) é
  COMPARTILHADA com a Story 5.6, mesma decisão, NÃO resolver duas
  vezes.** Se a Story 5.6 já foi implementada, esta Story herda a
  correção em `BuscaLoja.tsx`/`BuscaProduto.tsx` sem repetir a revisão
  de @architect — ver detalhamento completo na Story 5.6 (Dependencies),
  não duplicado aqui.
- **Gap de ordem visual (AC2 do épico) — restrito a `BuscaProduto.tsx`**
  (a tela mista que mostra produtos E lojas). O texto do épico pede
  "exibe cards de loja ACIMA de produto" quando o termo bate em ambos;
  hoje `BuscaProduto.tsx` renderiza a seção "PRODUTOS" **antes** da
  seção "LOJAS" — ordem invertida em relação ao AC2. `BuscaLoja.tsx` (a
  variante focada só em loja, acessada por "Ver todas"/categoria da
  Home) não tem esse problema, pois não mostra produtos. Corrigido por
  esta Story (inverter a ordem das duas seções em `BuscaProduto.tsx`).
- `useSearchLojas` já implementa a busca (filtro de texto client-side
  case-insensitive + filtro de categoria) sobre `store.port.listByHub` —
  nenhuma port/adapter nova necessária.
- `estabelecimentos_hubs` (Story 5.2) — mesma sujeição à pendência
  BR-HUB já registrada (lista vazia até a associação existir); não é um
  gap novo desta Story.

## Complexidade

LOW — nenhuma port/adapter nova; a correção de escopo de hub é herdada/
compartilhada com a 5.6; o único ajuste específico desta Story é a
ordem visual das seções em `BuscaProduto.tsx`.

## Scope

**Incluído:** inverter a ordem das seções "LOJAS"/"PRODUTOS" em
`BuscaProduto.tsx` (lojas primeiro, conforme AC2); confirmar/herdar a
correção de escopo de hub da Story 5.6 (se ainda não aplicada, aplicar
aqui — decisão única, não duplicada).

**Excluído:** Edge Function/RPC de busca dedicada; ranking por
relevância; qualquer port/adapter nova.

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm qa", "typecheck do Cliente", "testes unitários/component da ordem de seções em BuscaProduto.tsx"]
preferred_execution_model: "terra"
model_fallback: "luna"
architecture_review_model: "sol"

## Story

**As a** cliente,
**I want** buscar por nome de loja e ir direto ao catálogo,
**so that** eu encontre uma loja específica que já conheço.

## Acceptance Criteria

1. Mesma tela de busca da Story 5.6 — já implementada
   (`BuscaProduto.tsx`/`BuscaLoja.tsx`). [Fonte:
   `docs/prd/epics/5-descoberta-busca.md` Story 5.7 AC1]

2. Se o termo bate com nome de loja, exibe cards de loja **acima** dos
   de produto — **corrigido por esta Story** em `BuscaProduto.tsx`
   (ordem atual: Produtos → Lojas; ordem alvo: Lojas → Produtos).
   [Fonte: `docs/prd/epics/5-descoberta-busca.md` Story 5.7 AC2,
   corrigido]

3. Toque no card de loja abre a Story 5.4 (catálogo) — já implementado.
   [Fonte: `docs/prd/epics/5-descoberta-busca.md` Story 5.7 AC3]

4. Se o termo bate com ambos, exibe seção "Lojas" e "Produtos"
   separadas — já implementado (`BuscaProduto.tsx` já renderiza as duas
   seções com título próprio); só a ORDEM é ajustada por esta Story
   (AC2). [Fonte: `docs/prd/epics/5-descoberta-busca.md` Story 5.7 AC4,
   ordem ajustada]

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend (ordem de seções + escopo de hub herdado)
**Secondary Type(s)**: Nenhum
**Complexity**: Low

### Specialized Agent Assignment

**Primary Agents**:
- @dev (Terra preferido; Luna como fallback).

**Supporting Agents**:
- Nenhum — decisão de guard/hub já resolvida pela Story 5.6, não
  repetida aqui.

### Quality Gate Tasks

- [x] Pre-Commit (@dev): `pnpm qa`, typecheck do Cliente.
- [ ] QA (@qa): validar ordem visual (lojas acima de produtos) e o
  escopo de hub herdado da Story 5.6.
- [ ] Pre-PR (@devops): somente com autorização explícita do usuário.

### CodeRabbit Focus Areas

**Primary Focus**:
- Ordem de renderização das seções `LOJAS`/`PRODUTOS` em
  `BuscaProduto.tsx`.

**Secondary Focus**:
- Nenhuma duplicação da decisão de escopo de hub já resolvida na Story
  5.6.

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only (registrado para @qa)

## Tasks / Subtasks

- [x] **[@dev]** Inverter a ordem de renderização das seções em
  `BuscaProduto.tsx` — seção "LOJAS" antes de "PRODUTOS" (hoje é o
  inverso). (AC: 2, 4)
- [x] **[@dev]** Confirmar que a correção de escopo de hub
  (`DEFAULT_HUB_ID` → `cart.hubId` + guard, Story 5.6) já se aplica a
  `BuscaLoja.tsx`/`BuscaProduto.tsx` — se a Story 5.6 ainda não foi
  implementada, aplicar aqui, mas SEM repetir a decisão de @architect
  (já registrada na 5.6). (AC: 1) — implementada na mesma sessão
  (Story 5.6), herdada aqui sem duplicação; decisão de guard registrada
  UMA vez no Dev Agent Record/Change Log da 5.6.
- [x] Teste de componente confirmando a nova ordem visual
  (Lojas → Produtos) em `BuscaProduto.tsx` quando o termo bate em
  ambos. (AC: 2, 4) — **não implementado como teste automatizado**:
  `apps/cliente` não tem harness de teste de componente React
  configurado (mesmo gap registrado no Dev Agent Record da Story 5.4;
  `vitest.config.ts#include` só cobre `src/**/*.test.ts`). A troca de
  ordem é puramente de JSX (duas `<View>` invertidas) — verificada por
  leitura de código + `typecheck`; confirmação visual/manual pendente
  (mesmo tratamento do mission brief para E2E/device).
- [x] Rodar `pnpm qa`, `pnpm --filter @keepit/cliente typecheck`.

## Dev Notes

### Contexto e limites

- A troca de ordem em `BuscaProduto.tsx` é puramente de apresentação —
  não altera `useSearchLojas`/`useSearchProdutos` nem os dados
  retornados, só a ordem de `<View>` no JSX. [Source:
  `apps/cliente/src/screens/home/BuscaProduto.tsx`]
- Reforço: a decisão de escopo de hub (`DEFAULT_HUB_ID` → `cart.hubId`)
  é a MESMA decisão da Story 5.6 — não reabrir nem duplicar a revisão de
  @architect se a 5.6 já resolveu.

### Testing

- Teste de componente da ordem de seções.
- `pnpm qa`.
- `pnpm --filter @keepit/cliente typecheck`.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-12 | 0.1 | Draft SIMPLE criado a partir do Épico 5 (Story 5.7) e do overlay do piloto. Único ajuste específico desta Story: ordem visual das seções "LOJAS"/"PRODUTOS" em `BuscaProduto.tsx` (hoje invertida em relação ao AC2). Escopo de hub (`DEFAULT_HUB_ID` → `cart.hubId`) é decisão COMPARTILHADA e ÚNICA com a Story 5.6, não duplicada aqui. Nenhuma port/adapter nova. | @sm (River) |
| 2026-08-12 | 0.2 | Validated GO (9/10) — Status: Draft → Ready. Ajuste único (ordem visual Lojas-antes-Produtos em `BuscaProduto.tsx`) é fix de apresentação real, alinhado ao AC2 do épico ("cards de loja acima de produto"). Decisão de escopo de hub explicitamente herdada da 5.6 (não reabrir a revisão de @architect) — coerência entre stories preservada. Sem ranking inventado, sem port nova. | @po (Pax) |
| 2026-08-12 | 0.3 | Status: Ready → In Progress → In Review. Ordem das seções invertida em `BuscaProduto.tsx` (LOJAS antes de PRODUTOS, AC2/AC4). Escopo de hub + guard herdados da Story 5.6 (mesma sessão, mesmas duas telas) — decisão de @architect NÃO duplicada. `pnpm qa` 27/27, `pnpm --filter @keepit/cliente typecheck` limpo. | @dev (Dex) |
| 2026-08-12 | 0.4 | QA gate CONCERNS — Status: In Review → Done. Confirmado por leitura do JSX: seção "LOJAS" (`BuscaProduto.tsx:116`) antes de "PRODUTOS" (`:129`) — AC2/AC4 atendidos. Mudança puramente de apresentação; dados/hooks inalterados. Escopo de hub herdado da 5.6 sem duplicação (mesmo bloco de guard). Reexecução fora do cache Turbo: cliente 48/48; typecheck exit 0. 1 débito low: ordem visual sem teste de regressão automatizado (sem harness RN; device pendente). Gate: `docs/qa/gates/5.7-busca-resultados-loja.yaml`. | @qa (Quinn) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Dex, @dev)

### Debug Log References

Nenhum log de debug fora do padrão. `pnpm --filter @keepit/cliente typecheck` limpo; `pnpm qa` 27/27 tasks verdes.

### Completion Notes List

- Ordem das duas `<View>` de seção em `BuscaProduto.tsx` invertida:
  "LOJAS" agora renderiza antes de "PRODUTOS" (AC2/AC4) — mudança
  puramente de apresentação, `useSearchLojas`/`useSearchProdutos` e os
  dados retornados não foram alterados por esta Story.
  `BuscaLoja.tsx` já não tinha o problema (não renderiza produtos).
- Escopo de hub (`DEFAULT_HUB_ID` → `cart.hubId` + guard) implementado
  na mesma sessão pela Story 5.6 (mesmos dois arquivos) — herdado aqui
  sem repetir a decisão de UX do guard (já documentada no Dev Agent
  Record/Change Log da 5.6).
- Sem teste de componente automatizado para a ordem visual — gap de
  infraestrutura já registrado (Story 5.4): `apps/cliente` não tem
  harness de teste de componente React configurado. Verificado por
  leitura de código (troca de posição das duas `<View>`) +
  `typecheck`; confirmação visual/manual pendente.
- CodeRabbit não pôde ser executado neste ambiente não-interativo —
  seguiu o gate determinístico `pnpm qa`.

### File List

- `apps/cliente/src/screens/home/BuscaProduto.tsx` (ordem de seções
  invertida — LOJAS antes de PRODUTOS, AC2/AC4; escopo de hub +
  guard registrados/implementados pela Story 5.6, mesmo arquivo)
- `apps/cliente/src/screens/home/BuscaLoja.tsx` (escopo de hub + guard
  herdados da Story 5.6, sem mudança adicional nesta Story)

## QA Results

**Gate: CONCERNS** — @qa (Quinn), 2026-08-12. Gate file: `docs/qa/gates/5.7-busca-resultados-loja.yaml`.

### Resumo

Ajuste único desta Story (ordem visual LOJAS→PRODUTOS) confirmado por leitura direta do JSX. Escopo de hub herdado da 5.6 sem duplicação. Reexecução fora do cache Turbo: **cliente 48/48**, typecheck exit 0.

### 7 Checks

1. **Code review** — OK. `BuscaProduto.tsx`: bloco "LOJAS" (linhas 116-127) precede "PRODUTOS" (linhas 129-141). Troca puramente de JSX; `useSearchLojas`/`useSearchProdutos` e dados retornados inalterados. `BuscaLoja.tsx` não renderiza produtos (sem o problema). Guard/hub-scope = mesmo bloco herdado da 5.6, não reaberto.
2. **Testes** — OK com débito. A ordem visual não tem teste de regressão automatizado (sem harness de componente RN — débito 5.4/5.6). Verificado por code review + typecheck.
3. **ACs** — AC1/AC3 pré-existentes; **AC2 MET** (lojas acima de produtos); **AC4 MET** (duas seções separadas, só a ordem ajustada).
4. **Sem regressão** — reordenação não altera comportamento de busca; mock navegável; 5.1–5.6 intactos.
5. **Robustez** — cada seção condicional a `length > 0`; guard de hub herdado permanece fail-safe.
6. **Segurança/honestidade** — sem mudança de dados (só JSX); sem ranking inventado; busca por loja sobre a leitura pública escopada (RLS fail-closed / mock filtra ativo); sem duplicação da decisão de guard.
7. **Documentação** — Change Log/Dev Agent Record honestos.

### Issues

| ID | Sev | Categoria | Descrição |
|---|---|---|---|
| DEV-5.7-001 | low | tests | Ordem visual das seções sem teste de regressão automatizado (sem harness RN); verificada por code review + typecheck; device pendente. |

Nenhum critical/high. **CONCERNS → Done.**
