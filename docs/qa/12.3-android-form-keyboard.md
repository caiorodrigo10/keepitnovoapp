# Story 12.3 — Evidência Android de formulários e teclado

> **Registro histórico da tarefa.** O APK QA foi gerado posteriormente. Para o
> artefato atual e a decisão RC, use `docs/qa/epic-12-android-rc.md`. O conteúdo
> abaixo continua válido como prova de que nenhum smoke manual foi executado.

## Build verificado

**PENDENTE.** Nenhum build EAS foi iniciado nesta execução. O ambiente continua
com o bloqueio conhecido de resolução DNS/rede para EAS; por instrução, o
comando `eas build --platform android --profile demo-apk` não foi repetido.
Assim, não há ID de build, URL de artefato APK, data retornada pelo EAS nem APK
release verificável para registrar.

O perfil que deve ser usado na retomada é `demo-apk` em
`apps/cliente/eas.json`, configurado com `distribution: internal` e
`android.buildType: apk`.

Commit candidato aos gates automatizados: `82d0686984e2c681908e94360a68eb049d7764e2`.

## Ambiente

Execução automatizada em 2026-09-05 (UTC), no worktree
`/projects/keepitnovoapp/.worktrees/story-12-1-mock-cliente`.

Ambiente Android manual: **não disponível/verificado**. Modelo, API, resolução,
modo de navegação, presença do Gboard e escala de fonte não foram observados
porque não houve APK para instalar. Permanecem pendentes os dois percursos:

- Compacto: aproximadamente 360 × 640, gestos, Gboard e fonte 1,3.
- Grande: aproximadamente 412 × 915, três botões, Gboard e fonte padrão.

## Resultados

| Superfície/comportamento | Resultado | Observação |
| --- | --- | --- |
| Cadastro e termos acessíveis | PENDENTE | Requer APK, Gboard, gestos/fonte 1,3 e TalkBack. |
| Login, solicitar recuperação e redefinir senha | PENDENTE | Requer smoke no APK. |
| Edição de perfil, cartão e fallback de CEP | PENDENTE | Requer smoke no APK. |
| CPF em folha/modal | PENDENTE | Requer verificação de safe area e Gboard no APK. |
| CTA, foco, scroll, dismiss do teclado e voltar | PENDENTE | Requer percursos por gestos e três botões. |
| Controles obrigatórios com fonte 1,3 | PENDENTE | Requer percurso compacto após `adb shell settings put system font_scale 1.3`. |

Não foi atribuído `PASS` a comportamento de interface: nenhum deles foi
observado em um APK release.

## Evidências

As quatro capturas exigidas não foram produzidas porque o APK release não está
disponível. Não há arquivos de evidência para os caminhos abaixo:

- `docs/qa/evidence/12.3-cadastro-compacto.png` — PENDENTE.
- `docs/qa/evidence/12.3-cartao-compacto.png` — PENDENTE.
- `docs/qa/evidence/12.3-cpf-gboard.png` — PENDENTE.
- `docs/qa/evidence/12.3-termos-talkback.png` — PENDENTE.

Na retomada, gerar o APK com o perfil `demo-apk`, instalar com
`eas build:run --platform android --latest`, executar os percursos descritos na
story e salvar cada captura por `adb exec-out screencap -p` após navegar ao
estado correspondente. Restaurar a fonte com
`adb shell settings put system font_scale 1.0` depois do percurso compacto.

## Gates automatizados

| Comando | Exit code | Resultado resumido |
| --- | ---: | --- |
| `pnpm --filter @keepit/cliente test` | 0 | PASS — 29 arquivos e 211 testes; aviso existente de depreciação da API CJS do Vite. |
| `pnpm --filter @keepit/cliente typecheck` | 0 | PASS — `tsc --noEmit` sem erros. |
| `git diff --check` | 0 | PASS — saída vazia antes desta documentação. |

O teste de `formContracts` é um contrato puro: cobre a escolha da estratégia de
teclado, os props de scroll e a semântica do checkbox. O MVP não instala nem
executa renderer de componentes, portanto estes gates não comprovam foco, scroll,
layout, safe areas ou TalkBack. Esses cenários, assim como o AC 8, permanecem
pendentes de validação manual no APK.
