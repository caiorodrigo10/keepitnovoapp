# Story 12.4 — Evidência Android de header e acessibilidade

> **Registro histórico da tarefa.** O APK QA foi gerado posteriormente. Para o
> artefato atual e a decisão RC, use `docs/qa/epic-12-android-rc.md`. O conteúdo
> abaixo continua válido como prova de que nenhum smoke manual foi executado.

## Build verificado

**PENDENTE — consolidado na Story 12.14.** Nenhum build EAS foi iniciado nesta
execução e nenhum APK foi instalado. Conforme a decisão registrada no ledger
SDD desta story, os smokes Android das Stories 12.1–12.13 serão executados uma
única vez no gate de consolidação da Story 12.14. Portanto, não há ID de build,
URL de artefato, versão, commit SHA retornado pelo EAS ou data de build a
registrar para a Story 12.4.

Os commits de implementação verificados pelos gates automatizados são:

- `4cd0676` — componente e contratos do `AppHeader`.
- `8acdeb3` — estados estruturais de acessibilidade dos controles.
- `5ded805` — migração das telas tituladas para o `AppHeader`.

## Ambientes

Execução automatizada em 2026-09-05 (UTC), no worktree
`/projects/keepitnovoapp/.worktrees/story-12-1-mock-cliente`.

Ambientes Android manuais: **PENDENTES na Story 12.14**. Não foram observados
modelo/API do dispositivo, resolução, Gboard, TalkBack, escala de fonte nem modo
de navegação. A matriz que permanece pendente inclui:

- aproximadamente 360 × 640, fonte 1,3 e navegação por gestos;
- aproximadamente 412 × 915, fonte padrão e navegação por três botões.

## Headers

| Cenário | Resultado | Observação |
| --- | --- | --- |
| Variantes de título, subtítulo, badge e ação lateral | PASS automatizado | O contrato tipado é exercitado por `appHeaderContracts.test.ts`. Não substitui inspeção visual. |
| Retorno com histórico | PASS automatizado | `runHeaderBack` chama `goBack` uma vez e não chama o fallback. |
| Retorno sem histórico | PASS automatizado | `runHeaderBack` chama o fallback uma vez e não chama `goBack`; não foi criada rota artificial. |
| Centralização, quebra do título e slots de 48 px no aparelho compacto | PENDENTE | Requer observação no APK release com fonte 1,3, consolidada na Story 12.14. |
| Fluxos de Home, pedidos, Perfil e modais no Android | PENDENTE | Requer os percursos por gestos e três botões no APK release. |

## TalkBack e controles

| Cenário | Resultado | Observação |
| --- | --- | --- |
| Label/role/estado de controles laterais | PASS automatizado | O helper expõe role `button`, label e estado estrutural. |
| Botão loading ocupado/desabilitado | PASS automatizado | O contrato expõe `busy: true` e `disabled: true`. |
| Chips selecionados/desmarcados | PASS automatizado | O contrato expõe `selected` para o role `tab`. |
| Linhas selecionáveis marcadas/desmarcadas | PASS automatizado | O contrato expõe `checked` para o role `radio`. |
| Anúncios reais do TalkBack e ausência de submit duplicado | PENDENTE | Não houve APK nem sessão TalkBack; validação manual consolidada na Story 12.14. |
| Quebra de texto, sobreposição e alvos de toque no dispositivo | PENDENTE | Requer inspeção nas duas configurações Android da matriz consolidada. |

Nenhum comportamento de interface recebeu `PASS` manual sem observação em APK.

## Evidências

As capturas não foram produzidas, pois nenhum smoke de APK foi executado nesta
story. Não existem arquivos placeholder nos caminhos abaixo:

- `docs/qa/evidence/12.4-header-compact-font.png` — PENDENTE na Story 12.14.
- `docs/qa/evidence/12.4-header-three-button.png` — PENDENTE na Story 12.14.
- `docs/qa/evidence/12.4-selection-talkback.png` — PENDENTE na Story 12.14.

A Story 12.14 deverá associar as capturas ao APK release efetivamente instalado
e registrar versão, build, SHA, datasource, ambiente e data antes de emitir o
gate de release candidate.

## Gates automatizados

| Comando | Exit code | Resultado resumido |
| --- | ---: | --- |
| `pnpm --filter @keepit/cliente test -- src/components/ui/appHeaderContracts.test.ts src/components/ui/interactionAccessibility.test.ts` | 0 | PASS — 2 arquivos e 6 testes; aviso existente de depreciação da API CJS do Vite. |
| `pnpm --filter @keepit/cliente typecheck` | 0 | PASS — `tsc --noEmit` sem erros. |
| `git diff --check` | 0 | PASS — saída vazia após a documentação final. |

A suíte Cliente completa não foi repetida nesta tarefa: por decisão de escopo,
foram executados somente os dois testes focados e o typecheck. Os gates
automatizados comprovam os contratos puros e a integridade de tipos; não
comprovam layout renderizado, gestos, TalkBack ou comportamento do APK.
