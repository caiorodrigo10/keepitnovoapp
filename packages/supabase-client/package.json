{
  "name": "@keepit/supabase-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo skipped",
    "lint": "echo skipped",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "canary-check": "node --env-file-if-exists=../../.env scripts/canary-check.mjs"
  },
  "dependencies": {
    "@keepit/shared-types": "workspace:*",
    "@supabase/supabase-js": "^2.58.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "5.9.3",
    "vitest": "^1.2.0"
  }
}
